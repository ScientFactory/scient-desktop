/**
 * Lifetime shared by an exact generated-document capability and the durable
 * storage lease that keeps its immutable revision available for that token.
 */
export const ASSET_TOKEN_TTL_MS = 60 * 60 * 1_000;
