{
  "name": "@scientfactory/scient-skills",
  "version": "0.0.0-alpha.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./resolve": {
      "types": "./src/resolve.ts",
      "import": "./src/resolve.ts"
    }
  },
  "scripts": {
    "generate": "bun scripts/generate-catalog.ts",
    "generate:check": "bun scripts/generate-catalog.ts --check",
    "typecheck": "tsc --noEmit",
    "test": "bun run generate:check && vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
