# T3 alignment through 652515a34

Status: qualified pre-Antigravity alignment receipt. This record covers the exact ordinary upstream
range immediately before T3's official Antigravity ACP implementation. It does not activate that
implementation, authorize publication, or replace the separate migration decision gate.

Role: record the exact donor boundary, Scient compositions, and verification evidence for this
checkpoint. The ongoing procedure remains the [alignment protocol](upstream-alignment-protocol.md),
and current machine state remains [upstream-state.json](../../upstream-state.json).

## Exact boundary

- Owned repository: `ScientFactory/scient-desktop`.
- Owned base: `784b88f891223c2e1273de8fc84291db12453f25`.
- Previous official boundary: `5b8445b7a777ab1070aa97b062b1618971073a96`.
- Official donor: `pingdotgg/t3code`.
- Frozen target: `652515a349741d234111b85f27597be3265d1ffc`.
- Range: 5 commits; 24 donor-touched paths; 9 paths overlapping Scient work; 2 textual conflict
  resolutions.
- The target is five commits after `v0.0.39-nightly.20260903.1267`; no exact official tag points to
  the target.
- Branch: `codex/t3-sync-pre-antigravity-20260903`.
- History-preserving merge: `afc5f4f2a6072a3c61af1063be7ea2eebeb4347a`, with the owned base as
  first parent and the exact official target as second parent.
- Official tip observed during the subsequent Antigravity analysis:
  `fff33f9e851912363c5b1f3ac65598be35eb5f0d`.

Local `main` was clean and matched `origin/main` at the owned base before the dedicated worktree was
created. `origin` remains the writable Scient remote; `upstream` remains fetch-only, with push URL
`DISABLED`.

## Received behavior

- Trailing assistant tool groups remain visible instead of being folded into the `Worked for`
  accordion.
- Claude slash-command expansion also works when a message carries attachments.
- Resting composer measurement uses the stable upstream layout and no longer oscillates between
  measured states.
- Assistant-generated image parts render inline in the conversation timeline.
- The marketing site receives the named-developer quote update.

## Composition decisions

The two textual conflicts were in `ChatComposer.tsx` and `MessagesTimeline.tsx`. Both were resolved
by composing the upstream structure with the existing Scient product seams rather than choosing one
whole side.

### Composer

Upstream owns the stable measurement helper, compact resting layout, context-strip keying, and the
new trailing-tool/image rows. Scient keeps assisted-provider setup and update controls, provider
switching and fork safeguards, voice and bidirectional-text behavior, and rich-output composition.
No provider authentication, installation, or browser flow starts from mount or measurement.

### Timeline

Upstream's assistant-image row and trailing-tool grouping are retained. Scient's checkpoint-aware
conversation fork remains available from the assistant metadata row, and the row comparator includes
the fork capability so stale controls are not retained across state changes.

## Deferred official Antigravity boundary

The next official commit, `06336460c`, adds Google's official Antigravity ACP agent and is
intentionally not part of this checkpoint. It overlaps Scient's existing Antigravity provider at 47
textual conflicts, including 10 add/add provider-core and documentation files. The implementation is
valuable, but accepting it requires explicit migration decisions for credentials, legacy
conversation cursors, Intel macOS, arbitrary files, paired large artifacts, and running-session
updates. No portion was cherry-picked or hand-reimplemented here.

Scient's current Antigravity install, sign-in, runtime selection, update, repair, remove,
diagnostics, attachments, and conversation behavior therefore remain unchanged at this boundary.

## Verification

Local checks use Node `24.19.0` and pnpm `11.10.0`.

- Formatting, lint, typecheck, production build, desktop startup smoke, brand inventory, Scient
  analysis/onboarding/skills/LaTeX seam inventories, mobile static checks, whitespace checks, and
  exact ancestry checks pass. Lint retains the repository's advisory diagnostics.
- The focused Antigravity characterization suite passes 8 files and 91 tests before and after the
  merge.
- Web passes 479 files and 4,788 tests; mobile passes 137 files and 1,136 tests; desktop passes 30
  files and 347 tests. All package and relay projects pass.
- The first concurrent monorepo invocation produced one transient 404 while an unchanged
  `server.upsertKeybinding` test bootstrapped its test-only browser session. The test and route were
  not touched by this range. The exact test then passed 10 consecutive isolated runs, and the entire
  server project passed 389 files with 8 skipped and 4,637 tests with 40 skipped. A second complete
  `pnpm test` invocation then passed without recurrence.
- SwiftLint, ktlint, and detekt are unavailable on this host and are therefore not claimed.

The review does not perform a live provider login or turn, install a provider update, build native
iOS/Android applications, exercise Windows, or publish any product artifact. Those are explicit
non-claims.

## Publication boundary

This remains a local integration checkpoint until reviewed. It does not authorize a pull request,
push, merge into owned `main`, release, cloud activation, or mobile publication. The official
Antigravity ACP implementation remains a later phase requiring the recorded product decisions and
cumulative acceptance.
