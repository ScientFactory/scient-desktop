# Upstream alignment through `6c583620`

Status: history-preserving candidate approved for PR delivery after automated qualification and
owner-led manual review.

## Frozen boundary

- Scient base: `083993b13b42718873721fe90c92d8cd87fedd64`.
- Previous official integration: `12391bd0d38eef6655b7a9f8945d0cb5febadc2b`.
- Official target: `6c583620ff7ad3235b135af7107c0543467eecfa`.
- Target description: `v0.0.41-nightly.20260909.1426-4-g6c583620ff`.
- Range: 16 official commits, 111 upstream-touched paths; 25 overlap Scient changes.
- Actual text conflicts: 12 files.
- Branch: `codex/t3-sync-6c583620-20260909`.
- Merge: `72a99fadb7d42c34fd470d82827583b239335a46`, with the exact owned
  base and official target as its two parents.
- Official remote: `https://github.com/pingdotgg/t3code.git`; push URL: `DISABLED`.

No donor commit was replayed, cherry-picked, or squashed. Open upstream PRs are outside this range.

## Adoption and conflict composition

### Composer and provider contracts

Adopted shell-based branch controls, session-local checkout Git-status memory, pending-catalog
presentation, and context-meter space reservation. Scient retains its real-project requirement,
project-opening preload path, fork checkpoint availability, provider-unbound fork selection,
queue/steer controls, voice mounts, rich-editor departure handling, and opt-in composer collapse.

The pending catalog keeps a disabled model picker with the thread's stored selection; it does
not briefly substitute setup UI. Once the catalog arrives, Scient's existing assisted provider
picker remains authoritative for connection and installation. An already open setup stays mounted
through readiness refresh, retaining the composer focus scope and runtime-update send guard.

The optional upstream `reportsContextWindow` field composes with Scient's supported runtime
modes and rollback capability; no capability replaces another. Pi's adapter already emits context
usage, so its presentation now advertises it alongside Codex and Claude. Droid does not emit that
usage in its current adapter and is not advertised as doing so. Tests cover true, false, and omitted
presentation values alongside the Scient capability fields, plus Pi discovery.

### Wizards, project heading, and identity

Adopted the shared wizard header, panel, footer, and stepper for upstream setup flows. Flow state,
validation, managed provider setup, and Scient's independent Getting Started/import mounts remain
with their existing owners. The supported Pi driver is not restored as a disabled coming-soon item.
The draft heading adopts the text-only trigger while preserving workspace wording and scoped
project identity. Settings section navigation and ordering are unchanged.

Stable installer artwork adopts the neutral upstream palette and retains Scient text instead of
T3's mark. Nightly retains its existing Scient identity. Android/iOS device labels use the new
platform-aware implementation with Scient branding, including platforms with no reported version.

### Runtime, terminal, and mobile/relay

Adopted lazy native keyring loading for macOS cookie import and paginated event replay that releases
consumed pages. Replay ordering, cursor limits, cleanup behavior, and all existing immutable server
migrations remain unchanged.

Terminal links adopt ordinary-click preference routing and Cmd/Ctrl browser override, including
drag-versus-click discrimination. Scient's browser default and preview authorization remain.

Adopted mobile thread arrangement, shared notification priority/policy, fresh queued alerts,
permission-aware token refresh, current APNs routing, native HTTP-response compatibility, and Android
FCM alerts/activity. The relay keeps its iOS-only v1 device response and adds v2 for both platforms.
The timestamped Android relay migration adds an Android API column and permits absent iOS versions;
it does not collide with a shipped migration and was not applied to a live database.

The new Android diagnostic watcher explicitly skips historical null-project shells before project
lookup. This fixes the composed type error without restoring projectless creation or changing
historical decoding. The diagnostic script was not run against a real profile or device.

Regenerated the lockfile from combined manifests with pnpm 11.10.0. Changes propagate the upstream
Effect HTTP compatibility patch through Scient packages; no provider catalog or runtime update was
introduced.

## Protected boundaries and review

Reviewed text conflicts and auto-merged overlaps against both parents. Scient identity, storage,
managed-provider lifecycle, recent analytics fixes, Dock answer attention, desktop update controls,
scientific Markdown, forks, and retained user settings remain. There are no source changes to the
independent project-opening, analytics, badge, or update-presentation implementations.

Relay deployment and mobile publication jobs remain explicitly disabled. No new credentials,
callback registrations, production data, telemetry configuration, updater authority, background
service, signing/publication control, or live cloud deployment was activated. The Android operations
guide explicitly identifies its inherited-reference status and separate Scient activation gate.

No major architectural or product decision was required. This pass does not redesign providers,
notifications, onboarding, or the alignment tooling.

## Verification and acceptance

Verification uses Node 24.19.0 and pnpm 11.10.0:

- Full workspace tests: 19,889 passed, 108 skipped across 27 test tasks. Web: 6,929 passed;
  server: 6,255 passed, 63 skipped; desktop: 1,338 passed, 43 skipped.
- Focused composition tests: 229 web tests, 18 server tests, and 119 relay tests passed.
- Full workspace typechecks, production build, non-visual Electron startup smoke, preload bundle
  verification, local release smoke, formatting, lint, branding, and unused-code/export checks passed.
- Analysis, onboarding against the exact upstream target, Skills, LaTeX, provenance, and whitespace
  guards passed. The merge's second parent is the official target.
- A persistent isolated app starts from the merge's product tree with fresh candidate data. Backend
  readiness and main-window creation are logged; web and environment metadata return HTTP 200.
  Process ownership, development bundle identity/signature, and persistence were verified without
  computer use. The backend advertises agent activity publishing as disabled.

Advisory lint/Effect diagnostics and dependency peer/deprecation warnings remain; pass does not mean
warning-free. The integration record advances only after this automated gate and literal merge.
The first desktop test attempt encountered an incomplete Electron runtime in the fresh dependency
installation; the repository's Electron-runtime helper repaired it before the complete rerun.

Native Android/iOS devices, Windows/Linux behavior, actual FCM/APNs delivery, provider authentication,
and live-provider execution are not qualified by these Mac-hosted automated checks. The native
static inventory reports SwiftLint, ktlint, and detekt unavailable rather than claiming they passed.

The owner requested an isolated clean development app and will perform visual review. No computer
use or visual acceptance was performed by the agent. Manual review identified that the shared
provider editor had regressed to placing the subscription label inline with the authenticated email.
The candidate now restores the label as its own row above the account control for every provider,
with parameterized coverage for labeled Codex and Claude accounts and an unlabeled fallback. The
owner approved PR delivery after reviewing the updated candidate. Merge and release remain separate
gates.
