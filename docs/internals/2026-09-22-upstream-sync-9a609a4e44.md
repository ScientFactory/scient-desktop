# T3 upstream alignment through 9a609a4e44

Status: the history-preserving full-alignment merge is composed and locally
qualified. This receipt records the open delivery candidate; it does not
authorize publication, cloud activation, mobile publication, release
publication, or any change to `release/stable`.

## Frozen history and exact boundary

- Owned starting point: `195f8b5f0f185068e34d0f67fdbb5a8b3122b844`, the exact
  Scient `origin/main` tip used for this alignment.
- Previous official integration: `1de563c1491c7d82563e4553bf5bf689ce6adbb9`.
- Official target: `9a609a4e444ba739d6fcd607769d68b679b0bcc5`.
- Exact donor range: `1de563c149..9a609a4e44`, 12 official commits,
  69 files changed, `+1,980/-629` before Scient conflict composition.
- Official description: `v0.0.43-nightly.20260920.1990-58-g9a609a4e44`.
- Alignment branch: `codex/t3-sync-9a609a4e-20260922`.
- History-preserving merge: `3af839a2f053e7fff7150023387d1df3761a12f7`, with
  the owned starting point as first parent and the exact official target as
  second parent.
- The fetch-only `upstream` remote remains configured with push URL
  `DISABLED`.

This is one bounded full-alignment range. It is not a squash, replay,
cherry-pick, or split implementation. The only post-merge change in this
candidate is alignment bookkeeping plus one Scient-facing copy correction for
the incoming public schema description; it does not alter the upstream
submodule-setting semantics.

## Complete upstream commit ledger

All 12 commits in the frozen range are present in the merge:

| Commit                | Upstream behavior                                                                                                   | Scient treatment and alignment risk                                                                                                               |
| --------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `371b52d9da` / #12843 | Pull-request row actions resolve immediately, with action phases and stale-response reconciliation.                 | Adopted in the existing PR list/detail surfaces; high attention because it crosses row state, mutation refresh, and detail-panel presentation.    |
| `33cb911ceb` / #12949 | Disables iOS search autocorrect, capitalization, and spellcheck for model/search identifiers.                       | Adopted through the native `react-native-screens` patch; medium mobile/native risk, with the lock hash retained.                                  |
| `2c2fa8cd7d` / #12951 | Pull-request chips show the provider state icon.                                                                    | Adopted in the shared chip-part renderer; low-medium presentation risk.                                                                           |
| `2d377bfb1c` / #12950 | Pressing a button dismisses an open selection-action surface.                                                       | Adopted in the shared selection helper while preserving Scient's prevented-selection opt-in and regression coverage; high interaction attention.  |
| `d2a90b921a` / #12865 | Message-copy actions use the accurate “Copy message” label and consistent naming.                                   | Adopted unchanged; low risk.                                                                                                                      |
| `80d9c181da` / #12763 | Legacy `message-sent` events without `turnId` decode as a compatible null instead of stopping startup.              | Adopted as a backwards-compatibility fix; low-medium protocol risk, verified by contract coverage.                                                |
| `e1cbb70527` / #12745 | Locale-aware custom snooze calendars use the locale's first weekday.                                                | Adopted unchanged; low risk.                                                                                                                      |
| `76cc9b08f1`          | Bumps the mobile application version to 1.3.0.                                                                      | Adopted as release metadata only; it does not enable Scient mobile publication.                                                                   |
| `1262d2f3ab` / #12953 | `t3.json` can choose recursive, top-level, or disabled submodule initialization for new worktrees.                  | Adopted through contracts and server worktree setup; high workspace/checkout risk, covered by focused VCS and contract tests.                     |
| `742173a132` / #12954 | Project settings resolve `t3.json` values through a centralized precedence resolver.                                | Adopted as the shared resolver, preserving Scient's project/environment settings and new-thread behavior; high settings-composition risk.         |
| `0141bc2bf5` / #12955 | Settings UI and mobile propagation expose the submodule initialization modes.                                       | Adopted through the existing scoped settings surfaces; high cross-platform settings risk.                                                         |
| `9a609a4e44` / #12972 | Undo actions move from transient toasts into a compact sidebar notice with grouping, timeout, and `mod-z` handling. | Adopted inside Scient's existing expanded-sidebar footer seam; high interaction/identity attention, with Scient release/update controls retained. |

## Conflict composition

The merge had four textual conflicts, matching the pre-merge three-way
simulation. They were resolved at the smallest stable seams rather than by
taking either side wholesale:

1. `projectFilesQueryState.ts` retains Scient's workspace-scoped refresh
   signal, refresh functions, and revision-aware optimistic file behavior,
   while exposing upstream's shared `optimisticFileAtom` entry point.
2. `SidebarChrome.tsx` retains Scient's identity, release notes, provider
   update pill, architecture warning, utility menu, and collapsed-footer
   wrapper. Upstream's `SidebarThreadUndoNotice` is mounted inside that
   existing expanded-footer seam.
3. `useHandleNewThread.ts` adopts the upstream centralized project-settings
   resolver and `t3.json` read path, while retaining Scient's navigation-intent
   coordination, composer-content transfer, model carry-over, and toast
   behavior. The obsolete duplicate thread-environment resolver import was
   removed.
4. `selectionActions.test.ts` adopts the upstream button-dismissal coverage
   and containment behavior while preserving Scient's prevented-selection
   opt-in and suppressed-gesture regressions.

There were 31 overlapping paths in the range, but no other textual conflicts.
The overlap review checked the auto-merged settings, server, VCS, pull-request,
mobile, and timeline surfaces for accidental replacement of Scient-owned
behavior.

## Protected-boundary audit

- Scient identity, product labels, General Chat/project routing, provider
  lifecycle, scientific/compute surfaces, state roots, browser authorization,
  storage, telemetry safety, updater/release authority, signing, cloud/relay,
  and mobile-publication boundaries remain unchanged.
- The new submodule setting changes checkout preparation only; it does not
  widen project or server authority, and no migration was renumbered or reused.
- T3's new undo notice is mounted without replacing Scient's release/update
  controls. The expanded-sidebar placement intentionally follows Scient's
  existing collapsed-footer behavior.
- The incoming public schema copy was changed from “T3 Code” to “Scient” so
  the product-facing contract does not leak upstream identity; the setting
  name, values, precedence, and runtime behavior remain upstream-compatible.
- No files under `apps/web/src/scient` changed in this range. The `upstream`
  remote remains fetch-only, and no live user data, credential, installed
  stable app, release branch, or production workflow was changed.

## Qualification

The exact merge candidate passed:

- Focused changed-area coverage: 17 files, 761 tests.
- Full workspace tests: 28 workspace lanes passed. Key lanes included web
  (734 files / 8,563 tests), desktop (185 / 1,695), mobile (30 / 286), and
  server (524 / 7,595). Repository-reported skips remain documented by the
  existing test configuration; there were no failing assertions.
- `pnpm run fmt:check` across 5,604 files.
- `pnpm run typecheck` across all 29 workspace lanes, with only existing
  Effect suggestion diagnostics.
- `pnpm run lint`, with the existing advisory warning set and no errors.
- `pnpm run build`.
- `pnpm run test:desktop-smoke`.
- `pnpm run brand:check` across 2,176 product-surface files.
- Scient analysis, onboarding, skills, and LaTeX seam checks.
- Focused contract coverage after the Scient-facing copy correction.
- `git diff --check` on the candidate code.

The first aggregate test attempt failed only because a fresh Electron binary
was installed concurrently by desktop test workers. The isolated Electron
binary was repaired within this worktree; the desktop lane then passed
independently and the complete aggregate matrix passed on the rerun. No test
was weakened or skipped to obtain the green result.

Visual/manual acceptance was not performed in this pass. It remains a separate
review gate for settings, pull-request actions, sidebar undo behavior, mobile
settings/search, and new-worktree submodule modes.

## Publication boundary

This branch is locally ready for delivery as an alignment PR. It has not been
pushed, opened as a PR, merged into Scient `main`, set to auto-merge, or used
to publish a release. Those are separate authorized actions.
