# T3 upstream sync through 65b005f1

Status: locally verified integration candidate. This record does not authorize
a release or modify `release/stable`.

## Exact boundaries

- Owned repository: `ScientFactory/scient-desktop-next`
- Owned base: `5fe83b7adddc6294ae733029d6a0ef2739171649`
- Previous literal T3 boundary:
  `f5fce74169a5629f701aeb8c4535cab6f7bd3c92`
- Official T3 target: `65b005f1e4bfccb6a404b3b1e5bfa363d534ac2a`
- Latest official tag in ancestry: `v0.0.34-nightly.20260811.1064`
- Exact donor range: `f5fce741..65b005f1`, two commits
- History-preserving merge: `ffe5d604fb3af82148eb709c9422121c99a3c90c`
- Integration branch: `agent/t3-sync-65b005f1-20260811`
- Donor write boundary: the `upstream` push URL remains `DISABLED`

The target was freshly fetched from official `pingdotgg/t3code:main`. A final
remote check after local verification still reported `65b005f1` as official
`main`. Both received commits were already merged upstream; no open pull-request
head was imported by this range.

## User-facing advancements

1. `752acbf65` / T3 PR #5994 lets users Shift-click New Thread to create
   directly in the current project and explains the shortcut in the tooltip.
2. `65b005f1e` / T3 PR #5574 adds Copy Thread ID to thread context menus in the
   sidebar and chat header, with success and failure feedback.

## Friction measurement

The unmodified merge simulation was run before any resolution:

- T3 paths changed: 6
- Scient paths changed since the common T3 boundary: 644
- Same-path overlaps: 3
- Overlapping paths merged automatically: 2
- Text-conflict files: 1
- Conflict regions: 1
- Upstream paths with no Scient overlap: 3

The only conflict was `apps/web/src/components/Sidebar.tsx`. It was necessary,
not incidental formatting or import churn. Upstream treated one configured
project as a single-target setup, while Scient must still offer a target picker
when General Chat adds a projectless target.

The resolution preserves both behaviors:

- Plain click still opens the target picker whenever General Chat is available.
- Shift-click creates directly in the current project when one exists.
- A projectless-only environment does not advertise or execute a nonexistent
  current-project action.
- The tooltip exposes the direct-create hint whenever both project and General
  Chat targets exist.
- T3's `Sidebar.logic.ts` helper and its upstream tests remain upstream-shaped.
- The projectless exception and its characterization tests live under the
  Scient-owned module, subsequently renamed `scient-quick-chat`.

This small range cannot prove that every future T3 update will be conflict-free,
but it provides useful evidence that the earlier separation work is functioning:
five of six incoming paths needed no resolution, and the sole conflict mapped to
a deliberate product difference rather than avoidable Scient edit placement.

## Verification

The integration candidate completed:

- focused Sidebar, target-picker, thread-menu, and General Chat tests: 129 passed
- focused web typecheck: passed
- repository formatting: passed
- repository lint: passed with one pre-existing unused-disable warning in
  `ThemeEditorPanel.tsx`
- full monorepo typecheck: passed
- full monorepo tests: all 18 tasks passed; the server suite reported 2,605
  passed and 7 expected skips
- production build: passed
- desktop smoke: passed
- release smoke: passed
- Scient brand check: passed across 1,127 product-surface files
- generated icon check: all 25 assets current
- mobile native static check: passed; optional local SwiftLint, ktlint, and
  detekt executables were not installed and reported their normal skips
- `git diff --check`: passed

The full gate was rerun after the final separation self-review. Hosted CI and
manual acceptance remain PR gates; neither is represented as complete by this
local record.

## Publication boundary

This synchronization targets owned `main` only. It does not modify or merge
`release/stable`, publish desktop/mobile artifacts, enable T3 relay deployment,
or change Scient release authority.
