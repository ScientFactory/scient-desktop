# T3 alignment through 01e64193

Status: automated qualification passed; owner manual review pending. This is one full,
history-preserving alignment from the previously integrated official boundary through the exact
official target below. It is not a split, cherry-pick, squash, or replay.

## Frozen history

- Owned base: `8a293f0836684c6e381d1b228e3aa02e2ea360ac`.
- Previous official integration: `c1b2210411da79350a3f9f5a65a4a64971e4acad`.
- Official target: `01e64193d9d2abfae8f7b3d20f66824e38397bd6` (53 first-parent commits after
  the previous official integration).
- Nearest target tag: `v0.0.43-nightly.20260916.1825`, eight commits before the target.
- Branch: `codex/t3-sync-d1a64489-20260916`.
- Initial history-preserving merge: `65d6a5100ced1098f91b1c7256a70f5d9616bca3`; its first parent is the exact
  owned base and its second parent is `d1a644897119300688cbd8bf26e0b64aeb92c382`.
- First extension merge: `a0628627d8da25788af9d8c200ba0751ac189562`; its second parent is
  `32e8b2584556c0c55ce0d1d8f72b506cb771d60d`.
- Review-loading extension merge: `157a2a167b23cf53e60583256172351780059f43`; its second
  parent is `aae361c4ef6478e4dcf970d574f3fd38d908e6b9`.
- Proactive-panel extension merge: `664c68a103d6e5713b902f99565f6bcbd086589d`; its second
  parent is `52ad70ec43b5dcfcdc72ac79c0964da8e4d163db`.
- Final extension merge: `00e25964c2625c0beac6b54d08af861afd0a49dc`; its first parent is the reviewed
  alignment candidate and its second parent is the exact final official target.
- `upstream` remains fetch-only (`https://github.com/pingdotgg/t3code.git`, push `DISABLED`).

## Adopted behavior

- Composer settings now support the inherited Enter or Command/Ctrl+Enter send shortcut and the
  queue-or-steer follow-up preference. Scient maps those choices onto its existing durable
  server-authoritative queue rather than introducing a second browser-local queue.
- Provider transcripts can include explicit reasoning rows. OpenCode, Cursor, and Grok expose their
  native usage limits; supported providers expose native slash-command catalogs. Scient's private
  product awareness is still appended through each provider's reviewed protocol-specific seam.
- Local folder drops become workspace path chips, while remote environments refuse a path they
  cannot safely resolve. Desktop paste-as-text avoids duplicate insertion.
- Project monograms gain customizable soft tints and a dedicated icon variant. Pull-request icons,
  narrow-panel controls, comment submission, diff-tree ordering, and tool/turn timestamps use the
  current shared upstream presentation.
- Mobile receives thread rename, deep-link hydration, stable measured chat rows, neutral
  incompatible-environment presentation, retained machine icons, native license search, safer bare
  app links, and updated Reanimated/Worklets dependencies.
- Client discovery records server compatibility and disables incompatible environments without
  misrepresenting them as offline. Older-page loading retains cached turns across refreshes.
- Checkpoint capture reuses Git index metadata, GitHub refreshes consume less quota, unanswered
  preview requests release their hosts, and standalone helper scripts resolve a suitable Node
  runtime through shared infrastructure.
- Antigravity health checks stop leaving PyInstaller `_MEI` directories behind. The cleanup is
  composed with Scient's verified managed-runtime lifecycle and passive-probe policy.
- Service preflight rejects legacy launcher protocols. Stable-channel mechanics remain confined to
  main, while Scient's own release, signing, updater, and publication authority stays unchanged.
- Pull-request Markdown can resolve GitHub-hosted images and videos from private repositories
  through the repository's existing `gh` credential. Signed asset URLs, strict GitHub host/path
  validation, redirect credential stripping, media-only response validation, and bounded private
  caching keep this inside the existing asset authority.
- Provider settings respond to their actual content width rather than only the window viewport.
  Scient retains its models-first provider editor and established setup/status hierarchy.
- Large Git reviews expose complete file and line totals even when the combined patch exceeds the
  preview budget. Web and mobile load individual file patches progressively, surface per-file
  loading or failure states, and retain reveal, annotation, and source-scope behavior.
- Proactive right-panel behavior prioritizes linked pull requests over completed-turn diffs.
  Automatic diffs open only for at least three changed files or 50 changed lines; later user panel
  choices still take precedence over every automatic request.
- Agent approval prompts keep Approve and Decline visible, move secondary provider choices into a
  compact menu, preserve warnings and full request details, and adapt at narrow composer widths.

## Deliberately retained Scient architecture

- Upstream's browser-local queued composer was not introduced. The inherited follow-up controls
  produce either a normal durable queue send or an explicit server `steer` request, and queued-item
  steering still targets the server queue item by ID.
- Scient's signed npm-pinned server runtime, provider install/update/repair/remove lifecycle,
  project-first conversations, product awareness, scientific surfaces, state roots, analytics
  consent, and browser authorization remain authoritative.
- Upstream release workflows, Scient-facing standalone-install copy, marketing download policy,
  hosted cloud activation, and mobile publication automation were not adopted. The inherited-host
  README remains upstream-shaped, while the inert mobile preview profile and compatible client
  contracts remain available without creating a publication path.
- T3's CLI installer/update progress remains in literal ancestry but is inactive in Scient. Its
  archive-based public installer and CLI updater were already deliberately removed; Scient retains
  the signed npm-pinned managed server runtime rather than creating a second installation path.
- No migration was added or renumbered. Scient's immutable migration sequence through `55` remains
  unchanged.

## Conflict composition and review findings

The initial merge presented 37 conflicted files plus substantial auto-merged overlap; the two-commit
first extension presented seven more textual conflicts, and the review-loading extension one. The
final proactive-panel extension merged without textual conflicts and received a semantic audit.
The final two-commit extension presented five conflicts at the intentionally removed CLI installer
and the signed pinned-runtime seam.
Resolutions retain upstream mechanics and keep Scient policy at narrow seams:

- Provider drivers combine upstream usage, native commands, reasoning, and Node-runtime discovery
  with Scient awareness, managed-runtime provenance, generated images, citations, and safe passive
  probing.
- Route leaves use the upstream shared thread-route architecture; Scient's project availability
  guard runs only after the shell is initialized so retained project-first routing cannot cause an
  early redirect.
- Timeline composition adopts stable setup headers, reasoning rows, and timestamps while preserving
  Scient's durable post-handoff setup-script status. A post-handoff script failure has one embedded
  row rather than a duplicated setup card.
- Desktop preload composition keeps both upstream file-path extraction and Scient clipboard/file
  bridges. Review removed duplicate `getPathForFile` definitions and retained the null-safe form.
- Release and product documentation conflicts retain Scient's publication and installation policy;
  compatible generic code remains upstream-shaped.
- The private-media extension reuses Scient's signed asset route rather than creating a parallel
  server path. Its renderer extends the existing Scient image implementation instead of duplicating
  it, preserving scientific figures, captions, workspace opening, PDF/media actions, and fallbacks.
- The provider-width extension changes shared container breakpoints while leaving Scient's
  models-first information architecture intact, avoiding a permanent duplicate provider header.
- The progressive-review conflict adopts complete counts and lazy file loading while retaining
  Scient's compact metadata typography. The shared diff/server contracts stay upstream-shaped;
  no parallel review model or Scient-only loading mechanism was introduced.
- The proactive-panel extension keeps the shared upstream arbitration and thresholds. Existing
  thread scoping and the right-panel user-action revision guard continue to protect Scient Sources,
  artifacts, browser/device panels, terminals, files, and other explicit user choices.
- The final extension adopts the shared approval presentation inside Scient's existing composer.
  It retains the CLI installer deletions and Scient's npm-pinned runtime implementation instead of
  grafting T3 archive progress onto an unrelated managed-runtime protocol.

Semantic review found and corrected issues that Git did not report as conflicts: a shared Node
runtime expected the upstream `HostProcessIsExecutable` service missing from Scient's extended host
process module; assistant-only completion fields were read from reasoning completion commands; PR
refresh accepted a nullable project ID; one environment-registry test double lacked the new
compatibility method; OpenCode and Antigravity fixtures did not model retained Scient awareness and
managed lifecycle composition; and new launcher and compatibility guidance asserted upstream
branding. Extension review additionally caught an inherited path-limit name that did not exist in
Scient, a duplicated Markdown image renderer, and a non-exhaustive Scient image-identity switch for
the new asset kind. Each correction is narrow and covered by the relevant typecheck or focused test.

## Qualification

- Formatting, lint, recursive typecheck, dependency/export analysis, production build, desktop
  smoke, release smoke, brand, and repository diff-integrity checks passed. Lint reports only
  existing non-blocking React and Effect suggestions.
- The complete recursive workspace test gate passed. Web passed 682 files and 7,787 tests; server
  passed 490 files and 6,818 tests, with 15 files and 63 tests skipped by declared conditions;
  desktop passed 1,414 tests with four files and 43 tests skipped; mobile passed 1,588 tests. Shared,
  contracts, scripts, and all other workspace packages passed as well.
- Analysis, onboarding, Skills, and LaTeX seam checks passed. Lockfile regeneration and supply-chain
  policy passed during installation. Provenance is checked against the committed merge graph after
  this receipt advances the recorded integration boundary.
- After the final two-commit extension, formatting, lint, recursive typecheck, dependency/export
  analysis, and the production build passed again. There were 426 focused passing test executions
  across signed assets, HTTP range behavior, server composition, Markdown/workspace images,
  provider settings, and static-image identity. The full recursive test gate above applies to the
  initial 47-commit candidate; the narrowly scoped extension was requalified through these focused
  tests plus full typecheck and build rather than rerunning unrelated suites.
- The final review-loading commit passed 125 focused server/web/mobile tests plus the complete
  77-test mobile review suite. Formatting, lint, recursive typecheck, dependency/export analysis,
  and the production build passed again after its merge.
- The final proactive-panel extension passed the complete web unit suite: 682 files and 7,798 tests,
  including pull-request priority, diff thresholds, and Scient-owned right-panel behavior.
- The approval extension passed 249 focused composer, right-panel, and proactive-panel tests. The
  final composed candidate then passed the complete web suite: 682 files and 7,797 tests.
- Mobile native source discovery passed. SwiftLint, ktlint, and detekt are unavailable on this host
  and were reported as skipped.
- No computer use, visual acceptance, live-provider qualification, Windows execution, or native
  iOS/Android visual review was performed.

## Owner manual review

1. In composer settings, switch the send shortcut and follow-up behavior. Verify normal sends queue
   durably, steering affects the active turn, and queued items still edit, steer, resume, and remove
   through the existing Scient strip.
2. Use a provider that emits thinking traces and native slash commands. Confirm reasoning rows,
   command selection, model selection, Scient awareness, and final responses remain coherent.
3. Drop a folder into a local-project composer and verify a path chip appears. Repeat against a
   remote environment and confirm it is refused clearly rather than attaching an unusable path.
4. Review provider usage for OpenCode, Cursor, and Grok, including refresh, unavailable data, and
   narrow layouts. Recheck Antigravity install/probe/update behavior and temp cleanup if practical.
5. Check project monogram tint selection, sidebar/header buttons, PR panels, diff-tree ordering,
   comment submission, and tool/turn timestamps in both light and dark themes.
6. On mobile where available, test thread rename, cold deep links, bare `t3code://` links, connection
   compatibility rows, measured chat alignment, license search, and usage display.
7. Recheck Scient-specific forks, provider setup, scientific pages, preview authorization, browser
   file handling, and release/update presentation around the touched shared surfaces.
8. Open a pull request from a private GitHub repository containing uploaded and committed images or
   video. Verify media renders, seeking/retry work, public media still falls back safely, and opening
   the original does not expose credentials.
9. Resize Settings > Providers and its content pane through narrow and wide widths. Verify fields
   wrap without horizontal overflow and the existing models-first tabs, status, setup, and manage
   controls remain coherent.
10. Review a branch and dirty working tree with enough large files to exceed the combined diff
    preview limit. Verify total counts remain complete, files load progressively on expansion,
    failures stay local to one file, reveal/annotation still work, and web/mobile agree.
11. With proactive panels enabled, finish turns with a linked pull request and with small and large
    diffs. Verify the pull request opens first, changes below both thresholds stay closed, larger
    changes open the diff, and any manual panel choice remains selected.
12. Exercise provider and MCP approval requests at wide and narrow composer widths. Verify Approve
    and Decline stay visible, secondary choices and warnings remain accessible in the menu, the
    full request detail is readable, and queue, voice, and attachment controls remain coherent.

Manual acceptance, pull-request creation, merge to `main`, cleanup, and release publication remain
separate owner decisions.
