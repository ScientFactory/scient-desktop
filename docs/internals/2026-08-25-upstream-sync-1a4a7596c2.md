# T3 upstream sync through 1a4a7596c2

Status: locally verified integration candidate. This record does not authorize
a release, enable cloud or mobile publication, or modify `release/stable`.

## Exact boundaries

- Owned base: `4ad62e5ebfc2995bab1876fa90e52d8edaf03c86`
- Previous literal T3 boundary:
  `99960383d094b850f0001d3067fa604aa3adf7d6`
- Integrated T3 target:
  `1a4a7596c2c557fcc26a5dceefc04ac6268640b4`
- Official tag at the target: none (nearest description:
  `v0.0.34-nightly.20260825.1182-5-g1a4a7596c2`)
- Exact donor range: `99960383d0..1a4a7596c2`, 11 commits
- History-preserving merge:
  `bff68c898dcce3fc73b5881df98e97c34702f0db`
- Integration branch: `codex/t3-sync-1a4a7596-20260825`
- Donor write boundary: the `upstream` push URL remains `DISABLED`

The merge commit has the exact owned base and official target as its parents.
No donor commit was replayed, squashed, or cherry-picked. `integrationBase`
advances to the merged target; `reviewedThrough` remains the separate
product-review cursor.

## Adopted behavior

- Routine server events no longer rescan complete thread history, and Cursor is
  not probed while its provider setting remains at the disabled default.
- pnpm installation remains lockfile-stable.
- Settled threads can be restored with the inherited keyboard shortcut, while
  shortcut hints wait briefly instead of flashing during ordinary navigation.
- macOS packaging batches signing calls and omits Windows-only terminal files.
  Scient's bundle identity, protocol, artifact naming, and signing authority
  remain unchanged.
- Threads can persist and display an explicitly linked pull request across web,
  mobile, snapshots, reducers, and remote clients.
- HEIC and HEIF attachments are dimension-checked, decoded lazily, and converted
  to bounded JPEG files before provider submission.
- Mobile authentication metadata includes OS major version and device model.
- Cumulative Grok tool output is bounded and coalesced before it can flood
  runtime events or persisted raw payloads.
- The release runbook now requires migration-aware remote-update and rollback
  acceptance.

## Conflict composition

The range changed 75 paths. Forty-one also changed on Scient's owned line since
the prior literal boundary; 15 produced textual conflicts. They were composed
against both parents rather than resolved by choosing an entire side.

- Scient's projection contracts retain `workspaceRoot`, fork lineage, historical
  project reassignment, and title-regeneration fields while adding T3's linked
  pull-request state to every full thread-row query and reducer path.
- T3's migration was assigned `044` because Scient's published `041`, `042`, and
  `043` migrations are immutable and remain byte-for-byte in place.
- The rich Markdown renderer keeps Scient's math, workspace images, diagrams,
  BiDi, scientific file surfaces, and safe-link behavior while adding the
  linked-pull-request context action.
- The sidebar keeps Scient's shared row implementation and existing thread
  behavior while adopting linked-pull-request status and delayed shortcut hints.
- The release conflict preserves Scient's full release and rollback policy and
  adds only T3's migration-specific acceptance requirements.
- The desktop build composes T3's batched macOS signing hook with Scient's
  identity, artifact names, native dependencies, and package exclusions.

The first changed-test pass found two composition omissions: an upstream test
fixture lacked Scient's required nullable `workspaceRoot`, and a Scient-added
thread query omitted the new linked-pull-request column. Both were corrected at
their contracts, then the affected and complete changed-test sets were rerun.

## Protected-boundary audit

- Scient identity, `scient-next` storage compatibility, protocols, preview
  partitions, client persistence, and legacy-data policy remain intact.
- Analysis, LaTeX, local voice, onboarding, browser-PDF export, scientific
  sources, provider lifecycle, durable forks, and assisted-provider mounts
  remain present.
- Telemetry, OTLP, cloud, relay, service activation, updater authority,
  signing credentials, release promotion, and publication gates remain
  unchanged.
- The managed and system-installed provider paths remain available; the Cursor
  change only avoids probing a provider that is disabled by default.

## Verification

Local non-visual verification used Node `v24.19.0` and pnpm `11.10.0`:

- All 27 changed test files passed, totaling 749 tests.
- `pnpm run test` passed all 24 workspace lanes. The complete server lane passed
  338 files and 3,604 tests, with 5 files and 27 tests skipped.
- `pnpm run typecheck`, `pnpm run fmt:check`, and `pnpm run lint` passed.
  Lint reported warnings but no errors.
- `pnpm run build`, `pnpm run test:desktop-smoke`, and
  `pnpm run release:smoke` passed.
- `pnpm run lint:mobile` completed its repository checks; optional local
  SwiftLint, ktlint, and detekt executables were not installed, so those three
  external checks remain hosted-CI/platform gates.
- Scient brand, onboarding, LaTeX, analysis, upstream provenance, exact
  ancestry, and `git diff --check` gates passed.

No browser automation, screenshots, visual acceptance, live provider action,
release publication, signing, notarization, or platform packaging was
performed.

## Publication boundary

This merge does not publish artifacts, modify `release/stable`, or enable T3
relay, cloud, hosted web, mobile store, signing, updater, npm, tag, nightly, or
release authority. Those remain explicit Scient gates.
