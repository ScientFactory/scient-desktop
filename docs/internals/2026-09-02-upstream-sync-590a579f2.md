# T3 alignment through 590a579f2

Status: qualified draft alignment receipt. The local automated gates passed;
the isolated desktop review and hosted pull-request checks are recorded before
this draft is considered ready. This record describes a proposed integration
boundary. It does not authorize merging to `main`, a release, or mobile
publication.

Role: record the exact donor range, Scient compositions, and verification
boundary for this alignment. The ongoing procedure remains the
[alignment protocol](upstream-alignment-protocol.md), and current machine state
remains [upstream-state.json](../../upstream-state.json).

## Exact boundary

- Owned repository: `ScientFactory/scient-desktop`.
- Owned base: `06e8489afc9e430da6cf1fd591f7f608a964141d`.
- Previous official boundary: `0947c30e6946b2ad6d6cd518fd44292e75e834e8`.
- Official donor: `pingdotgg/t3code`.
- Frozen target: `590a579f2e9292ce314c69e459e19620004578fe`.
- Range: 29 commits; 219 donor-touched paths; 83 paths overlapping Scient
  work; 24 textual conflict/resolution paths.
- No tag points exactly at the target. Its nearest-tag description is
  `v0.0.38-nightly.20260901.1250-5-g590a579f2`.
- Branch: `codex/t3-sync-590a579f-20260902`.
- History-preserving merge: `8877ad2ae525bf770b5c225f61b3cca9d79f3f19`.
  Its first parent is the owned base and its second parent is the frozen target.
- `origin` remains the writable Scient remote; `upstream` remains fetch-only,
  with push URL `DISABLED`.

Local `main` was clean and matched the exact owned base before the dedicated
worktree was created. Later upstream changes are outside this bounded range.

## Received behavior

- Server-owned settlement makes inactivity and pull-request settlement run
  without an open client, while preserving explicit un-settlement and active
  work. Scient keeps a seven-day inactivity default and keeps merge settlement
  off by default.
- Server performance work bounds activity-payload memory, scans only appended
  usage transcript bytes, coalesces chatty live frames, avoids redundant
  streaming reads, caches project favicons, and closes leaked provider event
  subscriptions and OpenCode child sessions.
- Claude model availability now comes from a remote manifest instead of a
  duplicated hardcoded list, including Claude Fable 5.1, with one shared
  catalog used by normal turns, text generation, and Scient voice correction.
- Desktop receives Electron 43 preview/recording repairs and configurable
  direct, hold, or double-press quit confirmation with migration from the old
  boolean setting.
- Web receives stable latest-command activity, dialog-safe type-to-focus,
  project settings from thread menus, project and environment preservation,
  pull-request panel synchronization, theme-state fixes, and a copy-thread-
  reference shortcut.
- Mobile receives updated scroll bounds, native keyboard commands, thread
  reference copying, and the server-owned settlement settings surface.

## Composition decisions

All 24 textual conflict paths and the automatically merged overlaps were
reviewed. The main seams were server settlement and projections, provider model
discovery, composer drafts, sidebar behavior, Settings ownership, preview and
recording, and cross-client contracts.

### Server authority without changing Scient policy

Upstream's server-owned settlement architecture replaces the retired
client-local timer. Scient's policy remains explicit: seven inactive days by
default and pull-request merge settlement off by default. Projectless
historical shells are excluded from automatic settlement. An open pull request,
active turns, pending input, background work, or a newer user action prevents a
stale automatic decision. Legacy client-local settlement keys are discarded;
the server owns the durable settings and broadcasts changes to all clients.

The orchestration composition preserves Scient fork hydration and legacy Codex
citation compatibility while adopting ID-first bounded activity reads,
server-side settlement events, live-event coalescing, and race guards around
running work. Provider authentication failures still propagate, and
non-assistant deltas remain excluded from assistant output.

### One Claude catalog across provider surfaces

The remote Claude manifest becomes the source for provider turns, lightweight
text generation, and voice-transcript correction. Scient retains its reviewed
manifest endpoint, managed effective environment, disabled provider self-
updates, and official CLI authentication readiness check. Custom model aliases
remain scoped; no passive probe starts authentication.

### Draft and sidebar continuity

Upstream's in-place draft retargeting is retained so choosing another project
does not create a duplicate draft. Scient's prompt, generic files, images,
terminal-context cleanup, sticky model behavior, and explicit model choices are
preserved. Cross-environment retargeting clears only stale upload authority so
the destination re-uploads safely.

The maintained sidebar receives server-owned settlement and upstream thread
actions while retaining Scient projectless visibility, Sources and other
Scient mounts, content-direction behavior, and project guards. A silent
auto-merge duplication of the sidebar renderer was removed during review.
The committed seam audit also caught that upstream's mobile new-task rewrite
had dropped the Scient project-skill inventory from unsent drafts. A narrow
follow-up now composes the same effective provider-skill inventory already used
by existing-thread composers, so slash and skill menus remain consistent.

### Desktop and client compatibility

The preview manager adopts upstream's Electron 43 capture path and recording
repair. The quit setting accepts old persisted booleans and writes the new
canonical mode. Client runtime bootstrap traffic is reduced through the shared
server-config projection without changing Scient storage roots or environment
authority.

## Protected boundaries

Scient product identity, `scient-next` storage roots, migrations, General Chat,
Sources, Skills, voice, analysis, compute, PDF, LaTeX, rich chat, content
direction, browser authorization, generic attachments, and managed/system
provider lifecycle remain intact.

No workflow, contributor trust, service, signing, updater, release, hosted web,
mobile publication, analytics delivery, OTLP, cloud, or relay authority is
enabled. Other worktrees and live user data were untouched.

## Verification

Local checks use Node `24.19.0` and pnpm `11.10.0`.

- Frozen dependency installation, formatting, lint, typecheck, brand checks,
  whitespace and conflict-marker checks, the production build, and desktop
  startup smoke passed. Lint retains advisory diagnostics already present
  outside this alignment.
- The complete monorepo run passed after the final composed assertions. Web:
  4,400 tests passed. Server: 382 files passed, 8 skipped; 4,438 tests passed,
  40 skipped. All mobile, desktop, relay, and package suites passed.
- Focused settlement, projection, provider-catalog, Claude voice/text,
  composer-draft, timeline, client-runtime, contract, preview, and desktop quit
  suites passed.
- Native mobile static checks passed; platform SwiftLint, ktlint, and detekt
  were unavailable and therefore not claimed. The native resource monitor
  passed 15 tests. Release-policy smoke passed.
- Provenance and Scient-owned seam checks are run against the committed merge
  and exact official target. An isolated synthetic-state desktop candidate is
  reviewed proportionally before the draft pull request is marked ready.

The review does not perform a live provider login, provider turn, microphone
capture, update installation, native iOS/Android build, or Windows runtime.
Those are explicit non-claims, not inferred passes. Mobile publication remains
disabled.

## Publication boundary

The branch is intended for a reviewed draft pull request. Opening that request
does not authorize merging it, publishing a release, activating retained mobile
features, or merging any other task's work.
