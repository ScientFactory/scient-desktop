# T3 upstream sync through 99960383d0

Status: locally verified integration candidate. This record does not authorize
a release, enable cloud or mobile publication, or modify `release/stable`.

## Exact boundaries

- Owned base: `ef3d1187faa43960c73300e16f68df00e3f5eccf`
- Previous literal T3 boundary:
  `e6a109b9f74552eb735968986845e500b98c3c73`
- Integrated T3 target:
  `99960383d094b850f0001d3067fa604aa3adf7d6`
- Official tag at the target: none (nearest description:
  `v0.0.34-nightly.20260824.1179-5-g99960383d0`)
- Exact donor range: `e6a109b9f7..99960383d0`, 7 commits
- History-preserving merge:
  `fd3e55eaf76758c463d472ae1ac78aed831535d1`
- Integration branch: `codex/t3-sync-99960383-20260825`
- Donor write boundary: the `upstream` push URL remains `DISABLED`

The merge commit has the exact owned base and official target as its parents.
No cherry-picks, replayed donor commits, feature work, dependency changes, or
user-data operations entered this lane. `integrationBase` advances to the
merged target; `reviewedThrough` remains a separate product-review cursor.

## Adopted behavior

- Composer update notices stay behind the attached composer surface instead of
  visually showing through it.
- Same-release nightly clients now detect older nightly servers, while stable
  and nightly builds with the same core version remain compatible.
- Usage loading skeletons match the loaded layout, and model breakdowns sort
  by token count when the token metric is selected.
- Terminal links advertise clickability only while the required modifier is
  held, and URL activation handles uppercase schemes consistently.
- Web and mobile recognize spaced relative filenames. Web chat also preserves
  Windows drive paths and backslashes through Markdown parsing and safely
  rejects unsafe schemes.
- Agent-authored source-file links open in the file viewer by default. Scient
  keeps PDFs on its range-aware file surface and workspace HTML in the
  integrated browser.

## Conflict composition and compatibility adaptations

One textual conflict in `apps/web/src/components/ChatMarkdown.tsx` was composed
explicitly. T3's normalized Windows path is used for duplicate-basename lookup,
while Scient's workspace-relative path remains available to its preview route.
A narrow compatibility adaptation also composes T3's generic file-viewer-first
rule with Scient's explicit workspace-HTML browser route. This retains correct
Windows labels, ordinary source links in the file viewer, workspace HTML in the
integrated browser, and PDFs on Scient's range-aware file surface.

Four other paths overlapped Scient changes and merged automatically:

- `markdown-links.ts` and its tests retain Scient's decoded lookup keys,
  workspace-root handling, and file-resolution policy while adding spaced
  filenames and the PDF-only browser-first predicate.
- `versionSkew.ts` and its tests retain the Scient update command and
  user-facing Scient guidance while adding full nightly ordering.

Scient's rich Markdown plugins, workspace images, math, BiDi, diagrams, LaTeX,
scientific file surfaces, and safe external-link handling remain composed in
the final renderer.

## Protected-boundary audit

- The range changes 17 inherited web/mobile files and no lockfile, package,
  workflow, migration, provider, desktop, server, or Scient-owned source root.
- Scient identity, `scient-next` storage compatibility, protocols, preview
  partitions, client persistence, and legacy-data policy remain intact.
- Analysis, LaTeX, local voice, onboarding, browser-PDF export, scientific
  sources, provider lifecycle, durable forks, and assisted-provider mounts
  remain present.
- Telemetry, OTLP, cloud, relay, service activation, updater authority,
  signing, release promotion, and publication gates remain unchanged.

## Verification

Local non-visual verification used Node `v24.19.0` and pnpm `11.10.0`:

- Focused Markdown, terminal, usage, composer-banner, version-skew, mobile-link,
  and Scient rich-renderer suites passed 15 files and 241 tests. The final
  HTML-routing compatibility adaptation then passed 5 focused files and 92
  tests before the full-suite rerun.
- `pnpm run test` passed all 24 workspace lanes. The complete server lane passed
  337 files and 3,594 tests, with 5 files and 27 tests skipped.
- `pnpm run typecheck`, `pnpm exec vp fmt --check`, and
  `pnpm exec vp lint --report-unused-disable-directives` passed. Existing
  suggestion-only and lint-warning diagnostics remain unchanged.
- `pnpm run build`, `pnpm run test:desktop-smoke`, `pnpm run release:smoke`,
  and `pnpm run brand:check` passed.
- Upstream provenance structure plus onboarding, LaTeX, and analysis seam
  verifiers passed. Exact upstream ancestry and `git diff --check` passed.

No browser automation, screenshots, visual acceptance, live provider action,
release publication, signing, or platform packaging was performed.

## Publication boundary

This merge does not publish artifacts, modify `release/stable`, or enable T3
relay, cloud, hosted web, mobile store, signing, updater, npm, tag, nightly, or
release authority. Those remain explicit Scient gates.
