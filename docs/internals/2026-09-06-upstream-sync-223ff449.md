# T3 alignment through 223ff4490f

Status: locally qualified candidate; human visual and interaction acceptance pending.
No push, hosted CI result, main merge, release, or publication is claimed by this record.

## Exact history

| Boundary                   | Value                                          |
| -------------------------- | ---------------------------------------------- |
| Owned base                 | `527dc96d84a62f5e64e034c09170c4776dafe188`     |
| Previous official boundary | `272d6d747ef214f50dbd9d0051120de12ffa3591`     |
| Official target            | `223ff4490f764a74ff911589e97b9bbcd595fee8`     |
| Range                      | 27 official commits                            |
| Target description         | `v0.0.39-nightly.20260906.1293-26-g223ff4490f` |
| Candidate branch           | `codex/t3-sync-223ff449-20260906`              |
| Literal merge              | `9c1eea0f9e8363315a090af54e2caf4a1a47986c`     |
| Latest owned main          | `7162724cc99585a20d7328396959b0ded33b79f2`     |
| Owned-main merges          | `0eaed32fac`, `ea12505196`                     |

The merge's first parent is the owned base and its second parent is the exact official target.
No donor commit was replayed or squashed. The merge changes 129 files relative to its first
parent. Initial textual conflicts affected 24 paths; auto-merged overlaps were also reviewed.
The official remote remains fetch-only. Later upstream movement is outside this frozen range.

While this candidate was being qualified, owned `main` advanced through provider PRs #245 and
#246 and shared-control PR #247. Those commits were incorporated with history-preserving merge
commits before final qualification. The provider merge had four textual conflicts: three were
older web/mobile copies of pending-request logic already centralized by this alignment, and the
fourth composed adapter-declared compaction with Scient's server-owned original-input contract.
The shared-control merge was textual-conflict free.

## Adopted behavior and conflict composition

- Adopt pooled provider usage limits on web and mobile, accessibility refinements, shared bulk
  deletion and request-outcome logic, and the upstream provider/session fixes.
- Adopt targeted turn-start and runtime-completion reads. Scient fork transcript preparation now
  loads full detail only for a thread carrying a fork-lineage marker; generated-image recovery uses
  targeted activity/message repositories and scans messages only when the target is unknown.
- Adopt upstream checkpoint-map ownership in the timeline and layer Scient fork markers/actions on
  that boundary. The chat view retains only the checkpoint lookup required for fork availability.
- Adopt server-owned pull-request discovery/linking, inactivity-first settlement, branch reuse
  protection, disabled-provider fallback, and checkpoint-before-PR-refresh ordering. Historical
  projectless threads remain excluded where repository context is required.
- Adopt adapter-declared context compaction and shared Claude outcome mapping while retaining
  Scient's Claude reauthentication detection and provider-specific managed binary resolution.
- Adopt upstream manual-panel protection and count Scient Sources and scientific artifacts as
  explicit user choices, so automatic diff/PR panels cannot replace them during the same turn.
- Keep Scient's release workflow and release documentation unchanged. Upstream automatic-nightly
  scripts and scheduling are not activated. Hosted copy uses the Scient display-name seam.
- Renumber the new upstream branch-PR migration to 51 because Scient already owns migrations 48
  and 49, and recent development builds may record the now-retired migration 50. No released or
  previously recorded migration is rewritten.
- Preserve Scient assisted provider lifecycle, managed/system installations, model/reasoning UX,
  queue and steering, projectless conversations, durable forks, generated images, rich output,
  Sources, Skills, voice, compute, Markdown/HTML, LaTeX/PDF, state roots, and product identity.

## Findings fixed during qualification

- A composed thread-detail SQL query initially omitted the new branch-PR projection column even
  though the decoder required it. The query now selects the column; focused reactor tests caught
  the issue before the merge was frozen.
- Fork-delivery reactor tests now construct actual fork lineage. This validates the new narrow
  ordinary-thread path instead of forcing every turn to hydrate full history.
- The upstream settlement deduplication test opts into automatic settlement on merge explicitly.
  Scient intentionally keeps that preference disabled by default.
- Scient Sources and artifact surfaces are now included in the manual-panel-choice regression
  matrix.
- The shared pending-request parser now keeps malformed choice-only requests closed instead of
  silently converting them into free-text questions. This preserves the stricter behavior added
  for Pi/Droid while retaining T3's single shared reducer.
- The incoming branch-PR migration moved from 50 to 51 after PR #246 added compatibility for
  development databases that may already record the retired migration 50. The compatibility
  suite now proves both histories reopen without rewriting recorded migrations.

## Verification

- Repository tests after incorporating owned `main`: all non-server packages passed in the full
  run; web passed 595 files / 6,341 tests, desktop 82 files / 879 tests with 20 skipped, mobile 27
  files / 214 tests, and client runtime 141 files / 1,263 tests. After resolving the migration-ID
  compatibility finding, the complete server rerun passed 441 files / 5,948 tests with 15 files /
  63 tests skipped.
- Repository typecheck, formatting, lint, Knip file/dependency/export checks, and full build passed.
  Lint and typecheck retain existing warnings/suggestions; no rule was suppressed.
- Desktop Electron smoke passed.
- Scient brand, analysis, onboarding, Skills, and LaTeX seam guards passed. The onboarding guard
  was run against the exact frozen upstream target because the local `upstream-verification` ref
  still points at an older boundary.
- Git diff checks, conflict-marker scan, and unresolved-index checks passed before the merge commit.

No native iOS/Android build or device test, live provider login/send, connected-machine test,
hosted cloud test, or visual acceptance is claimed.

## Manual acceptance focus

1. Usage limits with multiple accounts/environments on desktop; mobile only if a test device is
   available.
2. Start, complete, stop, and resume turns with Codex, Claude, OpenCode, and Antigravity; confirm
   completed approvals/user-input requests stay closed.
3. Let a turn produce a diff or PR panel, then manually select Files, Browser, Sources, and a
   scientific artifact and confirm the choice is not replaced during that turn.
4. Open a completed fork, inspect its baseline marker, and send a continuation message.
5. Check sidebar PR state and automatic settlement with the merge preference both off and on.
6. Verify generated images, timeline streaming, checkpoint rollback/fork actions, and ordinary
   projectless conversation behavior.
