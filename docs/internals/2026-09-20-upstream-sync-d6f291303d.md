# T3 upstream alignment through d6f291303d

Status: the extension of the history-preserving alignment is committed and the
automated/source review gates passed. This receipt records the delivery
candidate; it does not claim visual acceptance, release publication, cloud
activation, or new publication authority.

## Frozen history

- Owned starting point: `83dedda58a9f6bae8df25f55867af9f873090474`, the latest
  owned `origin/main` revision when this extension was prepared. It is present
  as the second parent of the owned-main catch-up merge
  `4e3ba3874bcdc7625a5ae53984609fb2f710396d`.
- Previous official integration:
  `b44c1ce5d25ee0d5a5be82e380618a886c19ea96`.
- Official target: `d6f291303ddc0c9a14f570266a4d9eff6d431593`. The range contains
  eight official commits after the previous boundary and touches 28 upstream
  paths (`+686/-140` in the aggregate upstream diff).
- Nearest official tag: `v0.0.43-nightly.20260919.1948`; the target is 14
  official commits after that tag.
- Alignment branch: `codex/t3-sync-b44c1ce5-20260919`.
- History-preserving alignment merge:
  `beb454891d22e5a80207df4f41976afd6a1f16f3`, with the updated owned branch
  as first parent and the exact official target as second parent.
- The fetch-only upstream remote remains configured with push URL disabled.
- This remains one bounded alignment extension. It is not a squash, replay,
  cherry-pick, or split implementation.

## Adopted upstream behavior

This extension receives the eight upstream commits in the exact range:

- the Providers settings heading is restored;
- desktop preview annotation screenshots are decoded locally from their PNG
  data URL instead of being fetched, avoiding CSP `connect-src` failures;
- typed text in a pending question is returned to the composer draft when a
  user selects an option, with cursor and trigger state restored;
- empty Claude `homePath` values resolve consistently with inherited
  `CLAUDE_CONFIG_DIR` or `~/.claude`, keeping continuation and capability keys
  stable;
- Flatpak and GTK4 SnapShot accessibility lookup can recover application text
  through the app list and a PID-scoped unnamed-window fallback;
- ACP startup/process failures retain a bounded, redacted stderr tail so users
  see the provider's real startup error instead of only a generic closed-session
  message.

The two upstream VOUCHED changes remain in ancestry but are not copied into
Scient's trust list. Scient deliberately grants trust entries individually.

## Deliberately retained or deferred

- Scient remains the product authority for identity, labels, state roots,
  provider lifecycle, scientific/compute surfaces, browser authorization,
  storage policy, privacy, and release decisions.
- The Scient fork-context bootstrap remains alongside upstream ACP process-error
  classification.
- Scient's managed Claude runtime resolution, inherited environment handling,
  subscription connection actions, and capability invalidation remain intact;
  only the upstream home-path environment is added to the cache-key inputs.
- Scient's pending-input draft handoff remains intact; the upstream shared
  composer-state helper is used for the final cursor/trigger restoration.
- No new provider, update channel, telemetry destination, cloud/relay authority,
  mobile publication workflow, signing behavior, or release policy was enabled.
- Migration history remains immutable; this range contains no migration or
  persistence-policy change.

## Conflict composition and semantic review

The updated branch first absorbed the latest owned `origin/main` through PR 318. The upstream merge then produced four textual conflicts:

- `.github/VOUCHED.td`: retained Scient's deliberate minimal trust list;
- `ProviderCommandReactor.ts`: retained the fork bootstrap and added the
  upstream process-error detail path;
- `ClaudeDriver.ts`: retained managed-runtime and connection-action behavior
  while passing the effective environment into the new cache-key resolution;
- `ChatComposer.tsx`: retained Scient's pending-input cleanup and composed the
  upstream cursor/trigger helper into that path.

The remaining overlapping files were auto-merged and reviewed, including the
preview annotation/CSP path, SnapShot accessibility path, ACP runtime, provider
settings, and question-input state. No conflict markers or unmerged paths
remain.

## Qualification

The following gates passed on the composed candidate:

- focused web tests: 4 files, 122 tests passed;
- focused server Claude/ACP tests: 5 files, 71 tests passed;
- focused desktop SnapShot tests: 2 files, 190 tests passed;
- focused `effect-acp` tests: 6 files, 52 tests passed;
- full `pnpm run test`: all workspace suites passed, including 543 server test
  files with 7,534 tests passed and 73 skipped, 724 web test files with 8,421
  tests passed, and 183 desktop test files with 1,679 tests passed;
- `pnpm run typecheck`, with only the repository's existing Effect suggestion
  diagnostics;
- `pnpm exec vp fmt --check`;
- `pnpm run build`;
- `pnpm run test:desktop-smoke`;
- `pnpm run brand:check` across 2,158 product-surface files;
- `pnpm run lint:mobile`; optional SwiftLint, ktlint, and detekt were not
  installed locally and were reported as skipped;
- conflict-marker, unmerged-index, and scoped whitespace checks.

Visual acceptance remains the owner's separate manual gate.

## Publication boundary

The existing PR is the delivery boundary. It remains open for review and CI;
no auto-merge, release, cloud, relay, mobile, or signing workflow is being
published or activated by this alignment.
