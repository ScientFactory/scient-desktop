# T3 upstream alignment through 1de563c149

Status: the history-preserving alignment merge is composed and source-level
review is complete. This receipt records the open PR delivery candidate; it
does not claim visual acceptance, release publication, cloud activation,
mobile publication, or new publication authority.

## Frozen history

- Owned starting point: `74341da96dfc54eb64fe26c2f2d6638f9b41f56a`, the exact
  owned branch tip from which this extension was merged.
- Previous official integration: `0ff87f251dafb32703d5f531e7b248ad0a581ee2`.
- Official target: `1de563c1491c7d82563e4553bf5bf689ce6adbb9`. The exact
  first-parent range contains 42 official commits and changes 186 files
  (`+10,229/-2,429`) before Scient conflict composition.
- Nearest official tag: `v0.0.43-nightly.20260920.1990`; the target is 46
  official commits after that tag.
- Alignment branch: `codex/t3-sync-0ff87f25-20260920`.
- History-preserving alignment merge:
  `d8b8f34ca1ccc5ecbdb21e3a79d584854324d3e6`, with the owned starting point
  as first parent and the exact official target as second parent.
- The fetch-only `upstream` remote remains configured with push URL `DISABLED`.
- This is one bounded full-alignment range. It is not a squash, replay,
  cherry-pick, or split implementation. The post-merge follow-up only updates
  stale contract assertions and alignment bookkeeping; it does not rewrite
  the merge or introduce feature work.

## Upstream behavior included in this alignment

All 42 official commits in the frozen range are present in the merge. They are
grouped here by user-facing and architectural area; grouping does not omit any
commit:

- **Device and managed-tool lifecycle:** installed/running tool-version
  visibility, read-only update discovery, remote ownership, automatic-update
  progress, host retry, simulator-stream recovery, safe reclamation of obsolete
  managed versions, version-catalog bumps, and the new manual update action in
  tool-version details.
- **Composer and interaction reliability:** controls stay visible while they
  fit, Escape dismisses suggestions, inline-code paste works inside emphasis,
  image chips do not appear for an empty composer, keyboard submit follows the
  primary worktree action, setup popovers are more compact, menu picks close
  correctly, and the resting composer no longer shifts the timeline.
- **Pull requests and source-control presentation:** linked pull-request row
  layout, refresh progress, shared hover previews, stable loading layout,
  current sidebar status, branch/path middle truncation, and preserved citation
  backslashes.
- **Workspace, sidebar, and undo behavior:** sidebar shelves move as a block,
  notification icons match status, thread unpin/settle/snooze/archive support
  undo, the `mod+z` shortcut is guarded outside editable surfaces, and the
  related mobile branch/worktree behavior remains correct.
- **Attachments and citations:** larger attachment counts are accepted without
  raising the image payload budget, and citation rendering remains composed
  with Scient's existing citation and queued-message path.
- **Desktop preview recording:** recording cursors align and input feedback is
  surfaced through the existing preview/recording seam.
- **Mobile review and device surfaces:** proper iOS pull-request iconography,
  diff word wrapping, source-worktree retention for branch starts, and safer
  iOS simulator shutdown behavior.
- **Accessibility and visual clarity:** assistant reply contrast can reach the
  full intended contrast, the composer hero exposes the project name to screen
  readers, narrow chat headers remain readable, and web/mobile presentation
  changes stay within their existing product surfaces.
- **Observability:** OTLP export configuration is represented per signal while
  remaining behind Scient's existing fail-closed telemetry envelope.

No official commit in the range was dropped. Scient's product decisions remain
the composition boundary: upstream behavior is incorporated where it fits the
existing architecture, while Scient-owned identity, scientific behavior,
provider lifecycle, privacy, storage, and release authority remain intact.

## Conflict composition and semantic review

The merge contained 20 textual conflicts. They were resolved at the smallest
stable seams rather than taking either side wholesale:

- desktop preview management retains Scient's HTML/PDF renderer boundary while
  receiving upstream recording input and lifecycle behavior;
- mobile labels and settings preserve Scient terminology while receiving the
  upstream accessibility and device interaction changes;
- server OTLP configuration keeps Scient's safety gate on every signal while
  adding upstream trace/metric/log configuration;
- pairing and server settings retain Scient's state-root, identity, custom-model,
  and provider-persistence behavior while taking upstream signal and setup
  mechanics;
- Git VCS retains Scient's no-Git `CommandAvailability` guard alongside
  upstream retry and worktree behavior;
- Codex adapter behavior retains citation and generated-image handling alongside
  upstream approvable-permission surfacing;
- the web composer, menus, settings, preview, pull-request, and timeline seams
  retain Scient's identity, General Chat/project routing, agent-only controls,
  and durable queue boundaries while taking the compatible upstream UI and
  state changes;
- user documentation and contracts were updated to describe the actual composed
  attachment/keybinding behavior, including the upstream contrast expression
  and the editor-specific shortcut guard.

The upstream historical client-side queued-message support was not copied as a
second queue implementation. Scient's durable server-authoritative queue
remains the single source of truth, as documented in
`docs/internals/scient-thread-queue.md`; compatible citation/rendering behavior
was composed into that existing path. This avoids two competing queue owners
without dropping the upstream user-facing behavior that fits the seam.

## Protected-boundary review

- No identity, product-label, state-root, provider-lifecycle, scientific/compute,
  browser-authorization, storage, migration, privacy, telemetry-destination,
  cloud/relay, signing, release, or mobile-publication authority was widened.
- OTLP traces, metrics, and logs remain fail-closed under the Scient safety
  envelope. The relay deploy path and unsupported publication channels remain
  disabled.
- Migration history remains immutable. No migration was renumbered or reused
  to match T3; the upstream migration sequence is not treated as Scient's
  synchronization counter.
- The review found no conflict markers, unmerged index entries, dangling
  references from upstream deletions, or duplicated queue owner.
- The vendored `.repos` reference synchronization remains verbatim, including
  upstream-owned trailing whitespace. The scoped non-vendored diff check passes.

## Qualification

The composed candidate passed:

- focused post-merge coverage: 35 files, 1,254 tests;
- corrected Markdown editor contract tests: 32/32;
- isolated server suite: 224/224 tests;
- web suite: 732 files, 8,496 tests;
- desktop suite: 185 files, 1,694 tests;
- mobile suite: 30 files, 286 tests;
- full build (`pnpm build`);
- full typecheck, with only the repository's existing Effect suggestion
  diagnostics;
- lint, with only the repository's existing advisory warning set;
- `pnpm exec vp fmt --check`;
- desktop smoke test;
- brand check across 2,172 product-surface files;
- mobile native static check (SwiftLint, ktlint, and detekt were not installed
  locally and were reported as skipped by the repository script);
- Scient analysis, onboarding, skills, and LaTeX seam checks;
- upstream provenance check and scoped `git diff --check`.

The root `pnpm test` orchestration was attempted twice. Both aggregate runs
reached the server workspace after all other workspace suites passed, then
stalled in the server process while another independent worktree was running
its own server/dev workload. The server suite passes independently in 35.25s
with all 224 tests, including the case that timed out under aggregate
contention. This is recorded as an environment-level aggregate-run limitation,
not hidden as a green full-matrix result; CI remains the authoritative clean
parallel run.

Visual acceptance was not performed in this pass because no dev app was
requested. The manual review gate remains separate.

## Publication boundary

PR #323 remains open intentionally so further official upstream commits can be
incorporated into the same alignment PR before merge. Merge, auto-merge, release
publication, visual acceptance, and any cloud, relay, mobile, or signing
workflow remain separate authorized steps.
