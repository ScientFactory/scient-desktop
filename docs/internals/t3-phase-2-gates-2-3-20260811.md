# T3 phase two Gates 2 and 3

Status: complete analysis checkpoint. No upstream merge, application refactor,
release-branch change, publication, or state-ledger advancement occurred in
this gate.

## Gate 2: exact integration lane

- Owned repository: `ScientFactory/scient-desktop-next`
- Initial Gate 2 base: `e43df2063181b47b16c2883196955d8b6124837b`
- Current PR base after merging PR #52 into `main`:
  `ee2ee6038ddb998b1710708538b677ce4d97e158`
- Initial base CI: successful GitHub `CI` run `31478819072`
- Integration branch: `agent/t3-phase-2-20260811`
- Worktree:
  `/Users/yaacov/REPOs/ScientFactory-worktrees/scient-desktop-next-t3-phase-2-20260811`
- Previous literal T3 boundary:
  `78f462c4e18c8ea5e5037dc916389a3b72246025`
- Pinned T3 target:
  `9c7622dac3d1a385351e6c74354a9e6b9c2037d5`
- Exact donor range: `78f462c4..9c7622da`, 15 commits
- Target tag: `v0.0.34-nightly.20260811.1064`
- Latest stable T3 tag in ancestry: `v0.0.33` at `3b72d17c`
- Upstream state at refresh: `upstream/main` equals the pinned target; there
  were no later official-main commits to separate.
- Donor authority: all 15 associated T3 pull requests are closed and merged
  into official `pingdotgg/t3code:main`; none is an open-PR head.
- Donor write boundary: the `upstream` push URL remains `DISABLED`.
- Release boundary: `origin/release/stable` remains
  `7c22a1d3408356b84a7e769c6269b8edbb6c2af7`.
- Existing Phase 1 head `eca3b41e` and T3 boundary `78f462c4` are literal
  ancestors of the owned base.

All 15 advancements are merged in official T3 `main` and included in the
nightly target. None is part of the stable `v0.0.33` release.

## Advancement disposition

| T3 commit / PR     | User result                                                                                                                                                                                                    | Gate 3 disposition                                                                                                                                                                | Risk                      |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| `cad2c936` / #4849 | Adds a Pull Requests page and in-thread PR tabs for GitHub, GitLab, Bitbucket, and Azure DevOps, including filters, code/timeline views, comments, reviews, merge/state actions, and project worktree handoff. | Mergeable with bounded Scient adaptations. It causes 11 conflict files. Most resolutions are additive, but provider writes and checkout handoff require the strongest validation. | High                      |
| `8de0aa24` / #5878 | Probes Windows shell environments concurrently, reducing startup delay.                                                                                                                                        | Clean adoption.                                                                                                                                                                   | Low                       |
| `b91a000a` / #6013 | Lets the user duplicate the built-in default theme before customizing it.                                                                                                                                      | Clean adoption; run branding and theme tests.                                                                                                                                     | Low                       |
| `0a7c662d` / #6000 | Improves built-in theme contrast and focus colors.                                                                                                                                                             | Clean adoption.                                                                                                                                                                   | Low                       |
| `1b120f35` / #6034 | Extends T3's GitHub-release publish timeout.                                                                                                                                                                   | Mergeable only as ancestry. Keep Scient's complete release workflow unchanged, so this commit has no operational effect in Scient.                                                | Low, intentional override |
| `428d9f9f` / #5613 | Allows Android tablets to rotate.                                                                                                                                                                              | Clean adoption; mobile publication remains disabled.                                                                                                                              | Low                       |
| `a7b0366c` / #6049 | Keeps the new PR-page header clear of Windows window controls.                                                                                                                                                 | Clean adoption with #4849.                                                                                                                                                        | Low                       |
| `9afef94a` / #5914 | Adds a three-hour thread-snooze choice.                                                                                                                                                                        | Clean adoption.                                                                                                                                                                   | Low                       |
| `e5c82d79` / #5988 | Keeps the Android composer above the gesture bar.                                                                                                                                                              | Clean adoption.                                                                                                                                                                   | Low                       |
| `2ea51bd3` / #5072 | Preserves OpenCode model identifiers containing `/`.                                                                                                                                                           | Clean adoption with focused parser tests.                                                                                                                                         | Low                       |
| `59d0c922` / #5136 | Persists collapsed Snoozed and Settled sidebar shelves.                                                                                                                                                        | Automatically merges, but retest the shared Sidebar with General Chat present.                                                                                                    | Low-medium                |
| `f9730979` / #5220 | Avoids expensive base64 work for oversized image candidates.                                                                                                                                                   | Clean adoption with image-compression tests.                                                                                                                                      | Low-medium                |
| `1fa315ea` / #5354 | Skips Linux libc detection on Windows and macOS.                                                                                                                                                               | Clean adoption.                                                                                                                                                                   | Low                       |
| `c14bcca1` / #5693 | Advertises 256-color terminal support on Windows.                                                                                                                                                              | Clean adoption.                                                                                                                                                                   | Low                       |
| `9c7622da` / #5944 | Makes VCS status work in a new repository before its first commit.                                                                                                                                             | Clean adoption.                                                                                                                                                                   | Low                       |

## Gate 3: authoritative merge simulation

The simulation used an ordinary two-parent merge from the exact owned base to
the pinned T3 target. It was then aborted; the worktree returned to the exact
owned base.

- Scient paths changed since the common T3 boundary: 636
- T3 paths changed in the target range: 167
- Paths changed by both sides: 32
- Text-conflict files: 12
- Conflict hunks: 16
- Overlapping paths that merged automatically: 20
- Upstream paths with no Scient path overlap: 135

### Conflict dispositions

| Conflict                                            | What collided                                                                                                                                                  | Preferred alignment                                                                                                                                                                                                                                                                   | Size / risk                    |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| `.github/workflows/release.yml`                     | T3's one-line timeout change is attached to a publication graph that contradicts Scient's manual, attested, fail-closed release graph.                         | Keep the Scient file byte-for-byte. Preserve the T3 commit only in ancestry. Do not hide this decision with a merge driver. Verify with `release:smoke`.                                                                                                                              | Small, operationally sensitive |
| `apps/server/src/server.ts`                         | T3 registers the PR HTTP route and shared PR service where Scient registers project initialization, analytics, and provider-lifecycle layers.                  | Compose all routes and both service families. Before the merge, consider one narrow Scient server-composition module so future T3 additions touch only one Scient mount.                                                                                                              | Medium, central composition    |
| `apps/web/src/components/ChatView.logic.ts`         | T3 narrows new-thread launch to `ScopedProjectRef`; Scient accepts `DraftThreadTargetRef` for projectless drafts.                                              | Keep the projectless-capable target and T3's broader promise return. Add a characterization test for starting a projectless thread.                                                                                                                                                   | Small, General Chat            |
| `apps/web/src/components/ChatView.tsx`              | T3 adds in-thread PR callbacks and PR-tab props beside Scient's file/PDF opening seam and projectless workspace availability.                                  | Keep `useScientFileOpening`, add T3's PR callbacks, use `activeWorkspaceRoot` for Files/PDF availability, and keep PR/worktree actions guarded by `activeProject`. Correct `reconcileFileSurfaces` to use workspace availability so General Chat file/PDF surfaces are not discarded. | Medium, General Chat/PDF       |
| `apps/web/src/components/DiffPanel.tsx`             | T3 extracts inherited diff-view styling into `StyledDiffCodeView`; Scient had changed three fixed metadata sizes to typography tokens in the old inline block. | Adopt T3's extraction and shared wrapper. Move Scient's token-only overrides to `scient/typography/profile.css` or another Scient-owned style seam, leaving `DiffPanel` upstream-shaped. Retest RTL and diff geometry.                                                                | Small-medium                   |
| `apps/web/src/components/LegacySidebar.tsx`         | T3 extends `openPrLink` to open an in-app PR tab; Scient retains multi-select behavior.                                                                        | Combine the new return/signature with Scient's optional selection flag. Projectless rows must fall back to the browser because they have no project PR context.                                                                                                                       | Small                          |
| `apps/web/src/components/RightPanelTabs.tsx`        | T3 adds PR-tab imports and environment capability state where Scient imports release notes and the Scient symbol.                                              | Keep both import families and T3's tab model. PDF remains a `file` surface, so the v11 PR-surface migration does not replace or rename PDF state.                                                                                                                                     | Small                          |
| `apps/web/src/components/Sidebar.tsx`               | T3 inserts a PR-aware `renderThreadRow` inside the project list; Scient already extracted one renderer so the separate General Chat section can reuse it.      | Keep the existing extracted renderer and incorporate T3's PR-link behavior there once. Retain `ScientGeneralChatSection` and `ScientGeneralChatPinnedList`; do not duplicate the upstream renderer. Adopt shelf persistence.                                                          | Medium, General Chat           |
| `apps/web/src/components/sidebar/SidebarChrome.tsx` | T3 adds the Pull Requests navigation/environment import where Scient imports release notes and product identity.                                               | Keep both; retain Scient identity and release notes while adding the PR entry.                                                                                                                                                                                                        | Small                          |
| `apps/web/src/hooks/useHandleNewThread.ts`          | T3 adds the returned `DraftId`; Scient imports the projectless draft key and target type.                                                                      | Keep all imports and the projectless-capable handler. T3 PR handoff still calls it with a real project.                                                                                                                                                                               | Small, General Chat            |
| `packages/contracts/src/environmentHttp.ts`         | T3 defines and registers PR-diff HTTP next to Scient project and analytics HTTP groups.                                                                        | Keep all groups. Extract Scient group definitions to a Scient-owned contracts module before merging if the rerun proves that it removes the large definition conflict without changing group names or client paths.                                                                   | Medium, contract surface       |
| `packages/contracts/src/rpc.ts`                     | T3 PR RPC imports land where Scient provider-lifecycle imports live.                                                                                           | Keep both import and RPC families. Avoid a generic plugin framework; extract only if a focused module measurably reduces the rerun conflict.                                                                                                                                          | Small-medium, contract surface |

## Clean semantic overlaps

Twenty same-path changes merge automatically but still require conscious
review:

- T3 adds 13 PR RPC methods. Existing methods keep their scopes; PR reads use
  orchestration-read, PR mutations and reviewer requests use
  orchestration-operate, and cache refresh remains read-scoped. No existing
  Scient endpoint is broadened.
- The HTTP PR-diff endpoint uses the existing authenticated environment layer.
  Scient project and analytics HTTP endpoints remain separately authenticated.
- The PR service uses existing host authentication wrappers. It introduces no
  new credential store. Existing Bitbucket compatibility environment-variable
  names remain internal compatibility names.
- PR handoff is project-backed. The default is a separate worktree; the local
  checkout choice is explicit. T3's refresh logic may move an existing PR
  checkout only when its HEAD is still the previously observed remote commit.
  This is the highest operational-risk clean merge and needs focused
  `GitManager`/`GitVcsDriver` tests plus manual disposable-repository proof.
- Right-panel storage moves from version 9 to 11, adds reference-keyed PR
  surfaces, and drops only the PR-list shared panel on restart. Scient PDFs use
  ordinary `file` surfaces, so their IDs and persistence remain compatible.
- T3's shared `StyledDiffCodeView` improves keyboard reachability and gives PR
  and thread diffs one geometry/styling adapter. Scient's RTL helpers in
  `diffRendering.ts` merge automatically but still require focused RTL tests.
- Theme contrast, default-theme duplication, GitHub-alert Markdown, image
  compression, Windows VCS/terminal changes, and mobile fixes merge without
  protected-boundary conflicts.
- The donor range contains no database migration. Its only persistence
  migration is browser-local right-panel state.
- The donor adds three visible upgrade messages that say `T3 Code server`.
  Replace those user-facing labels with Scient product copy while retaining
  compatibility identifiers such as `T3CODE_*`.
- T3's release-timeout change must not restore T3 npm publication, relay
  deployment, Blacksmith runners, or T3 GitHub-release authority.

## General Chat and the pending T3 exception

General Chat alignment is part of this update, not a separately deferred fix.
The target range does not modify the Scient-owned files under
`apps/web/src/components/scient-general-chat/`; it changes the inherited mounts
that those files use.

The current upstream state remains exceptional:

- T3 PR #5822, `feat(threads): add projectless threads`, is still open.
- Its exact head is
  `5566cfc166cebd9b207a541cd8551fba23a4fbda`.
- That exact head is already literal ancestry of Scient `main`.
- A paginated scan of all 738 current open T3 pull requests found #5822 is the
  only open PR head contained in Scient `main`.
- No additional #5822 commit is imported by the pinned Phase 2 target.

The PR-center implementation already requires a real project and repository
identity before opening an in-app PR or creating a PR worktree. General Chat
therefore retains file/PDF access through its environment workspace root but
does not receive project-only Git, PR, setup-script, or worktree actions.

If T3 later merges #5822 with a different final head, reconcile that official
history in a later bounded update. Do not rewrite Scient history now.

## Alignment-preparation outcome

The first implementation pass kept the donor target and release boundaries
unchanged while moving downstream behavior away from inherited T3 edit seams.

- Projectless draft launch now enters through a Scient-owned helper. The
  inherited project-only ChatView helper is upstream-shaped again, and an
  explicit test locks environment-scoped General Chat creation.
- General Chat file and PDF surfaces now use workspace availability rather
  than project presence. File tabs survive reconciliation when an environment
  workspace exists, while project-only operations remain separately guarded.
- Scient diff typography now extends the shared diff-rendering style seam that
  T3's replacement `StyledDiffCodeView` also consumes. `DiffPanel.tsx` is
  upstream-shaped at the block that T3 replaces. The other current consumer is
  file preview, whose `File` renderer disables its header and does not render
  diff-hunk separators, so the strengthened selectors have no current target
  on that surface.
- Scient server imports, HTTP-contract declarations, provider-lifecycle RPC
  imports, and shell imports are separated from inherited edit seams. Runtime
  layer order and top-level HTTP API group order remain identical to Scient
  `main`; no route, method, scope, service, or user-visible behavior was
  renamed.

A fresh ordinary two-parent merge simulation against the same pinned target
reduced the conflict surface from **12 files / 16 hunks** to **7 files / 10
hunks**. Conflicts were eliminated from:

- `apps/web/src/components/ChatView.logic.ts`
- `apps/web/src/components/DiffPanel.tsx`
- `apps/web/src/components/sidebar/SidebarChrome.tsx`
- `apps/web/src/hooks/useHandleNewThread.ts`
- `packages/contracts/src/rpc.ts`

The remaining seven files are the intentional release override, the central
server composition, three bounded PR/General Chat UI adaptations, the shared
Sidebar renderer, and one additive `EnvironmentHttpApi` composition hunk.
Keeping the server and contract ordering conflicts is deliberate: changing
Effect layer acquisition/finalization order or generated API group order only
to avoid merge overlap would create unnecessary regression risk. A generic
composition framework would likewise add more downstream divergence than
these explicit merge resolutions replace.

The preparation branch completed the repository verification gate:

- focused General Chat, ChatView, right-panel, diff-rendering, authorization,
  and contract tests: 92 passed
- repository formatting, lint, typecheck, full tests, and production build:
  passed
- desktop smoke and release smoke: passed
- Scient branding and generated-icon checks: passed
- mobile static check: passed; optional local `swiftlint`, `ktlint`, and
  `detekt` executables were not installed, so that command reported its normal
  native-linter skips
- `git diff --check`: passed

Lint retained one pre-existing unused-disable warning in
`apps/web/src/components/settings/ThemeEditorPanel.tsx`; it produced no error
and is outside this preparation diff.

A regression-first follow-up review restored the exact `main` ordering of the
Effect server layers and `EnvironmentHttpApi` groups. The corrected tree then
passed formatting, repository typecheck, `git diff --check`, and 210 focused
tests covering the server route harness, RPC authorization, General Chat,
right-panel state, and diff rendering. The review also checked the shared diff
CSS against both current consumers and the pinned T3 replacement adapter; the
strong selectors affect the interactive diff view and remain inert in the
headerless file preview. This intentionally accepted one extra conflict file
and two extra conflict hunks rather than preserving an unnecessary runtime
ordering change.

After PR #52 merged, this preparation branch rebased cleanly onto
`ee2ee6038`. PR #52's accepted-candidate release optimization remains entirely
base-owned: this PR still has no release-workflow diff. A fresh simulation from
the rebased head to the same pinned T3 target remained **7 conflict files / 10
hunks**, so the release update introduced no new T3-alignment conflict.

## Recommended implementation passes

1. **Behavior locks and low-risk seam preparation**
   - Add characterization tests for projectless draft creation, General Chat
     file/PDF availability, project-only PR guards, sidebar grouping, and the
     right-panel migration.
   - Move only the three Scient diff typography overrides into the existing
     Scient typography seam.
   - Extract Scient HTTP/server composition only when a rerun demonstrates a
     smaller conflict set.

2. **Contracts and server composition**
   - Integrate PR contracts, scope mappings, HTTP/RPC groups, provider registry,
     and shared service lifetime alongside Scient project, analytics, and
     provider-lifecycle services.
   - Rerun the merge simulation and require a real reduction before keeping
     any preparatory refactor.

3. **Web shell, including General Chat**
   - Integrate the PR-aware shared row renderer, PR navigation, ChatView tabs,
     file/PDF handling, shelf persistence, release notes, and branding.
   - Preserve the isolated General Chat section and move controller; fix
     alignment issues in the inherited mounts during this pass.

4. **Exact upstream merge and high-risk validation**
   - Merge the exact `9c7622da` target with literal ancestry.
   - Keep the Scient release workflow, resolve the reduced conflicts, and run
     focused plus full repository verification.
   - Exercise GitHub writes and PR worktree handoff only in a disposable
     repository. Treat GitLab discussion writes, Bitbucket writes, and Azure
     DevOps writes as documented live-E2E gaps unless safe test accounts are
     explicitly available.

## Required verification for the implementation gate

- Existing and upstream PR-provider, decoder, service, contract, scope,
  right-panel, PR-list/detail/review-store, and worktree tests.
- Scient provider lifecycle, analytics, project initialization, General Chat,
  file-opening/PDF, RTL diff, release-note, and release-smoke tests.
- Focused Windows shell, terminal, VCS unborn-HEAD, OpenCode parser, image,
  mobile, theme, and sidebar-shelf tests.
- `pnpm exec vp fmt --check`
- `pnpm exec vp lint --report-unused-disable-directives`
- `pnpm run typecheck`
- `pnpm run test`
- `pnpm run build`
- `pnpm run test:desktop-smoke`
- `pnpm run release:smoke`
- `pnpm run brand:check`
- `pnpm run icons:check`
- `pnpm run lint:mobile`
- `git diff --check`

Browser or computer-use acceptance remains outside this analysis gate and
requires explicit permission before the implementation gate uses it.

## Gate boundary

This report does not advance `upstream-state.json`. The integration base stays
at `78f462c4` until the exact donor ancestry is merged into an owned branch and
the resulting tree completes its verification gate.
