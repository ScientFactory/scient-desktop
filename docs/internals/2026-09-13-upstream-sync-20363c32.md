# T3 alignment through 20363c32

Status: automated qualification passed; owner visual review pending.
This is the final receipt for one complete alignment, extended in place from its interim a43f9b45
qualification point rather than split or cherry-picked.

## Frozen history

- Original owned base: `b9f26396fbf1754c16ecb50753c5e01a2232a36c`.
- Current owned main incorporated during the alignment:
  `39dd77cb5d2417f0c7c1eb6834e1bcafe8e60bba`, through history-preserving merge
  `42d774ca1399b1b8e68463e79c3644978ac0b285`.
- Previous official integration: `f814983c262b42bd79247bae377a709925c70d63`.
- Interim official target: `a43f9b45ae85caf37e0be8270ad3d27365ece2bd`, recorded in the
  [interim receipt](2026-09-12-upstream-sync-a43f9b45.md).
- Final official target: `20363c32c9bfdbf49c2716ef11d1f18483fcc01b`.
- Official range: 117 first-parent commits in total, including 34 added after the interim target.
- Target describe: `v0.0.41-nightly.20260913.1646-1-g20363c32c9`.
- Branch: `codex/t3-sync-a43f9b45-20260912`.
- Final history-preserving upstream merge: `dfa41f50b6943191256965eb83db440199d88108`,
  whose first parent is the composed owned head and whose second parent is the exact official target.
- `upstream` remains fetch-only (`https://github.com/pingdotgg/t3code.git`, push `DISABLED`).

## Additional upstream behavior adopted after a43f9b45

| Area                             | Included behavior                                                                                                                                                                                                                                                     |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Composer and work presentation   | Large pastes fold into text attachments while explicit paste-without-fold remains available; user input stays outside collapsed work; spawned subagents render as expandable work rows and remain visible under folded turns; chat messages expose semantic headings. |
| Notifications and usage          | Optional completion/input notifications and sounds, unavailable account limits made clearer, provider-account usage homes respected, and pooled mobile subscription widgets. Notifications remain off by default.                                                     |
| Source control and pull requests | Forgejo/Gitea support through `fj` and `tea`; optional trusted multi-account GitHub routing; provider selection in the PR toolbar; a floating PR comment composer; linked-PR navigation and diff defaults refined.                                                    |
| Files and media                  | On-demand folder loading and ignored-file browsing, raw-text word wrapping in Scient's richer preview surface, average-color image-chip tinting, viewer controls outside media, and restored arrow navigation.                                                        |
| Desktop and mobile reliability   | Native preview User-Agent preservation, bounded backend shutdown on quit, preview-focus recovery, bounded off-thread image conversion, launch-crash guards, startup diagnostics, stricter mobile TypeScript, and connection/environment refinements.                  |
| Providers and layout             | Grok monitor/background-shell lifecycle events, current Antigravity and provider mechanics, Cursor classic-IDE links, unified resizable panels, tighter sidebar layout, and complete thread-status indicators.                                                        |

The preceding receipt documents the first 83 commits and remains part of this final cumulative review.
All 117 official commits remain literal ancestry.

## Conflict composition and review findings

The extension reported 20 textual conflicts. Each conflict and material auto-merged overlap was
reviewed against the owned behavior and the exact official versions.

- **Scientific file surfaces:** keep Scient's `LazyWorkspaceTreeController`, internal-file policy,
  symlink/root protection, indexed search/reveal, PDF, Markdown, LaTeX, Sources, and Compute preview
  architecture. Port upstream's new raw-text word-wrap control into that architecture. The server's
  generic directory-entry RPC remains available for mobile and other clients.
- **Chat and composer:** compose Scient citations, BiDi direction, rich images, durable fork/rewind,
  and queue/steer behavior with large-paste attachments, semantic headings, live subagent rows, and
  upstream collapsed-work behavior. Keep the public product label as `Scient`.
- **Source control and settings:** adopt Forgejo/Gitea, GitHub routing, environment enable/disable,
  notifications, and diff defaults while retaining Scient's settings order, onboarding, provider
  lifecycle, scientific pages, release-note footer, and defaults. Trusted routing and notifications
  are opt-in.
- **Providers and server composition:** retain `ScientQueueWorkerLive`, capability-aware provider
  instructions, Antigravity presentation, and Scient protocol authority while adding repository
  identity, Forgejo layers, and current Grok lifecycle behavior.
- **Accessibility and image tests:** heading tests now prove both upstream semantic structure and
  Scient's intentional BiDi attributes. The zoomed-image keyboard test now supplies real horizontal
  overflow dimensions, matching the production guard instead of relying on zero-width happy-dom
  geometry.
- **Usage test portability:** upstream's usage fixture mixed macOS `/var` and canonical
  `/private/var` paths, causing one false assertion and one 120-second wait. Canonicalizing the
  temporary fixture root fixes both while leaving production path resolution unchanged.
- **Hermes compatibility:** the new upstream mobile lint rule exposed three retained Scient
  `.toSorted()` calls. They now use immutable copies plus supported `.sort()` operations, preserving
  ordering without relying on a Hermes-unsupported array method.
- **Shared contracts:** update the project-command registry test double for the new `setEnabled`
  method and replace two newly inherited public `T3 Code` strings with Scient wording. No behavior
  check was removed or loosened to obtain a pass.

## Protected boundaries

No cloud, relay, telemetry, mobile publication, updater, signing, notarization, tag, npm, release,
or deployment authority was enabled. Scient identity, isolated state roots, immutable migrations,
provider lifecycle, agent-awareness capabilities, Sources, Skills, voice, analysis, compute, PDF,
LaTeX, rich Markdown, and release holds remain. This extension adds no database migration and does
not reinterpret any migration ID.

## Qualification

- `pnpm exec vp fmt --check`, `pnpm exec vp lint --report-unused-disable-directives`,
  `pnpm typecheck`, `pnpm build`, `pnpm knip:check`, desktop smoke, release smoke, brand verification,
  and mobile lint: passed. Advisory Effect/lint diagnostics remain non-errors.
- Final web rerun: 672 files and 7,573 tests passed.
- Final server rerun: 481 files and 6,598 tests passed; 15 files and 63 tests were explicitly skipped.
  The broader monorepo run also passed desktop (167 files, 1,555 tests), mobile (30 files, 284 tests),
  relay (112 files and 1,390 tests, with existing skips), contracts, shared packages, runtime packages,
  scripts, and the scientific packages before the two server fixture failures were corrected.
- Analysis, onboarding, Skills, and LaTeX seam checks passed against the exact final official target.
- Repository integrity checks found no unresolved index entries, conflict markers, malformed diffs,
  or whitespace errors. The fetch-only upstream push guard remains intact.
- SwiftLint, ktlint, and detekt were unavailable and explicitly reported as skipped by the mobile
  static-check script.
- No computer use, live-provider message, Android/iOS device test, Linux/Windows runtime test, push,
  PR, main merge, release, or publication was performed.

## Owner manual review

1. Paste more than 32 KiB into the composer and confirm it folds into a text attachment; confirm the
   explicit paste-without-fold shortcut stays inline. Collapse a completed work block and verify user
   input and expandable subagent rows remain clear.
2. In Settings, confirm thread notifications and sounds default off and can be enabled. In
   Connections, disable and re-enable a saved environment and confirm trusted GitHub routing defaults
   off.
3. If available, review Forgejo/Gitea discovery. In pull requests, switch the provider selector,
   open a single linked PR, use the floating comment composer, and verify diff-file defaults.
4. Browse deep and ignored project files, search/reveal a result, and toggle word wrap in a raw text
   preview. Recheck Scient PDF, Markdown, LaTeX, Sources, and Compute previews.
5. Open an image gallery, use arrow navigation, then zoom until horizontal overflow exists and verify
   arrow keys pan rather than change images. Review panel resize persistence and sidebar spacing.
6. Recheck provider cards, Custom Models, Skills, Voice, project opening, queue/steer, fork, rewind,
   usage limits, and the existing scientific surfaces for regressions.

This receipt does not authorize a push, pull request, main merge, release, or publication. Owner
visual and interaction acceptance remains a separate gate.
