# T3 alignment through 0947c30e6

Status: qualified draft alignment receipt. The local automated and isolated
desktop review gates passed. This record describes the proposed verified
integration boundary; the boundary advances on `main` only if the draft pull
request is reviewed and merged. It does not authorize a release or mobile
publication.

Role: record the exact donor range, Scient compositions, and verification
boundary for this alignment. The ongoing procedure remains the
[alignment protocol](upstream-alignment-protocol.md), and current machine state
remains [upstream-state.json](../../upstream-state.json).

## Exact boundary

- Owned repository: `ScientFactory/scient-desktop`.
- Owned base: `83ffde24dc63c8d883613718aaec47b2362948e6`.
- Previous official boundary: `e4f7b14fab0850bff134a3f6bfaf5b71cc8ac9fc`.
- Official donor: `pingdotgg/t3code`.
- Frozen target: `0947c30e6946b2ad6d6cd518fd44292e75e834e8`.
- Range: 43 commits; 243 donor-touched paths; 83 paths overlapping Scient
  work; 29 textual conflict/resolution paths.
- No tag points exactly at the target. Its nearest-tag description is
  `v0.0.38-nightly.20260901.1243-4-g0947c30e6`.
- Branch: `codex/t3-sync-0947c30e-20260901`.
- History-preserving merge: `6c268b5ca942e03d3de6b661f75fae9f2ad546dd`.
  Its first parent is the owned base and its second parent is the frozen target.
- `origin` remains the writable Scient remote; `upstream` remains fetch-only,
  with push URL `DISABLED`.

Local `main` was clean and fast-forwarded to the exact owned base before the
dedicated worktree was created. Later upstream changes are outside this bounded
range.

## Received behavior

- Web receives the unified composer/activity presentation, persistent activity
  between actions, narrower metadata behavior, wide-table scrolling, GitHub PR
  links, pull-request filters, detailed Settings search, file-tree expand and
  collapse controls, work-log image rendering, and reusable Markdown image
  helpers.
- Mobile receives offline iPhone dictation, video and generic attachment
  composition/upload, native image/PDF/video viewing, Expo glass, corrected
  menu colors, safer post-submit navigation, and active-tool shimmer.
- Desktop receives Electron 43.4.1 and higher-quality browser recordings.
- Server and shared packages receive safer Claude probing, CLI event-origin
  compatibility, usage-cache preservation, long thread-ID routes, Windows PATH
  priority, isolated remote-session cookies, and lower memory loading for large
  tool-call outputs.
- The range also contains T3 release metadata, removal of the obsolete root
  `app.json`, and inert CodeRabbit configuration changes. Those commits remain
  literal donor ancestry without granting T3 release or review authority.

## Composition decisions

All 29 textual conflict/resolution paths and the automatically merged overlaps
were reviewed. The main seams were composer/activity presentation, rich chat,
mobile attachments and voice, desktop/browser integration, server HTTP
resources, shared contracts, and repository policy files.

### Unified interaction surface without losing Scient capabilities

The upstream composer surface and activity/banner architecture become the
maintained host. Scient voice dictation, queue/steer, Skills, conversation
forks, provider controls, direction hints, and historical plan chips are
mounted into that structure rather than restoring the retired component tree.
The separate upstream thinking row is retained, avoiding duplicate nesting.

The prior Claude compaction deferral was explicitly release-scoped. A focused
follow-up commit re-enables resume compaction after that release while adapting
it to the current composer and token-state architecture. It does not restore
obsolete context-window plumbing.

### Generic files, images, and rooted authority

Desktop and web continue to accept generic files; the mobile attachment menu
adds native video while preserving generic file selection and upload. No
image-only allowlist is introduced. Native mobile image, PDF, and video viewers
remain inherited capability, not an authorization to publish a mobile build.

Rich Markdown keeps Scient math, BiDi, authored image layout, standalone image
cards, rooted workspace resolution, generated artifacts, and sanitization while
adopting upstream GitHub PR links, work-log images, and shared image helpers.
Server HTTP handling composes upstream video byte ranges with Scient's generic
PDF/document ranges, revision semantics, and ETags.

### Security, identity, and lifecycle boundaries

Remote web cookies are partitioned per session as upstream intends. Existing
Scient browser partitions, rooted file access, provider lifecycle, managed and
system runtimes, storage roots, migrations, Sources, analysis, compute, PDF,
LaTeX, and release guards remain authoritative.

The removed root `app.json` is obsolete because mobile configuration is owned
by `apps/mobile/app.config.ts`. Existing T3 mobile identifiers were already
deliberately retained in unpublished mobile source; this alignment does not
expand them into publication authority. CodeRabbit review status and automatic
review remain disabled.

The production build exposed an upstream negated `@supports` expression that
compiled to invalid CSS. A focused follow-up groups the expression in the two
composer surfaces; the rebuilt CSS no longer emits those optimizer warnings.

## Protected boundaries

Scient product identity, storage roots, browser partitions, rooted assets,
migrations, project ownership, Sources, Skills, voice, analytics/consent,
analysis, compute, PDF, LaTeX, rich Markdown, content direction, and managed
provider lifecycle remain intact.

No workflow, contributor trust, service, signing, updater, release, hosted web,
mobile publication, OTLP, cloud, or relay authority is enabled. No passive
provider probe starts authentication. Other worktrees and live user data were
untouched.

## Verification

Local checks use the repository's pinned Node and pnpm toolchain.

- Frozen dependency installation, formatting, lint, typecheck across 27
  targets, brand checks, whitespace checks, and the five-target production
  build passed. Lint retains advisory diagnostics. The rebuilt web bundle has
  no invalid `@supports` optimizer warning.
- The complete monorepo test run passed. Server: 376 files passed, 8 skipped;
  4,375 tests passed, 40 skipped. Web: 465 files / 4,378 tests passed. Mobile:
  137 files / 1,067 tests passed. Desktop: 70 files / 688 tests passed, 10
  skipped. Relay: 27 files / 209 tests passed. All remaining package suites
  passed.
- Focused Claude compaction coverage passed: server 4 files / 170 tests, web 5
  files / 36 tests, shared 46 files / 393 tests, and contracts 29 files / 377
  tests.
- Desktop startup smoke passed.
- An isolated candidate used its own bundle ID, service, state root, backend,
  and web ports. Desktop review covered onboarding, Settings search by title and
  detail, provider/model defaults, provider update and account controls, the
  unified composer, attachment/voice controls, and an unrestricted native file
  picker. The picker accepted generic files and was cancelled without reading
  or uploading user data.

The desktop review did not perform a live provider login, provider turn,
microphone capture, or update installation. No native iOS/Android or Windows
runtime was compiled or exercised. Those are explicit non-claims, not inferred
passes. Mobile publication remains disabled.

## Publication boundary

The branch is intended for a reviewed draft pull request. Opening that request
does not authorize merging it, publishing a release, activating retained mobile
features, or merging any other task's work.
