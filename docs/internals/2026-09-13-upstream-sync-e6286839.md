# T3 alignment through e6286839

Status: automated qualification passed; owner visual review pending.
This is the final receipt for one complete alignment, extended in place through two additional
official reliability commits rather than split or cherry-picked.

## Frozen history

- Original owned base: `b9f26396fbf1754c16ecb50753c5e01a2232a36c`.
- Current owned main incorporated during the alignment:
  `39dd77cb5d2417f0c7c1eb6834e1bcafe8e60bba`, through history-preserving merge
  `42d774ca1399b1b8e68463e79c3644978ac0b285`.
- Previous official integration: `f814983c262b42bd79247bae377a709925c70d63`.
- Previous qualification target: `20363c32c9bfdbf49c2716ef11d1f18483fcc01b`, recorded in the
  [preceding receipt](2026-09-13-upstream-sync-20363c32.md).
- Final official target: `e6286839369a29d829aee3c64dd64ce39c626d2f`.
- Official range: 119 first-parent commits in total, including two added after the preceding target.
- Target describe: `v0.0.41-nightly.20260913.1658-2-ge628683936`.
- Branch: `codex/t3-sync-a43f9b45-20260912`.
- Final history-preserving extension merge: `df316cae0900dabaa6dffd62ed553d773ed20a12`,
  whose first parent is the previously qualified candidate and whose second parent is the exact
  official target.
- `upstream` remains fetch-only (`https://github.com/pingdotgg/t3code.git`, push `DISABLED`).

## Additional upstream behavior

- Unknown routes now render a focused “Page not found” state with a **Go home** action instead of
  leaving the user at a dead end.
- The root error screen's **Try again** action invalidates the router and repeats startup loading, so
  the app can recover after its server becomes available without requiring a full reload.

Both commits modify only the shared root route and use the existing branded display-name contract.

## Composition finding

The extension itself merged without textual conflicts, but semantic review of the shared root seam
found that the preceding upstream merge had mounted `ScientAnswerBadgeCoordinator` twice around the
new thread-notification coordinator. The duplicated mount could register duplicate answer-attention
effects even though typechecking and ordinary rendering remained valid. The final composition keeps
exactly one Scient answer coordinator and one upstream notification coordinator. No other root
coordinator, authentication gate, onboarding rule, tracing hook, or hosted-client behavior changed.

## Protected boundaries

No cloud, relay, telemetry, mobile publication, updater, signing, notarization, tag, npm, release,
or deployment authority was enabled. Scient identity, isolated state roots, immutable migrations,
provider lifecycle, scientific capabilities, and publication holds remain unchanged. The extension
adds no persistence or contract migration.

## Qualification

The cumulative qualification in the preceding receipt remains applicable to its exact integrated
history. This extension additionally passed focused root-route review, formatter, lint, typecheck,
web tests, brand and provenance checks, all four Scient seam checks against the final exact target,
desktop smoke, build, repository-integrity checks, and isolated-candidate readiness verification.
No computer use or visual acceptance was performed.

## Owner manual review

1. Open an invalid in-app route and confirm the branded “Page not found” state appears and **Go
   home** returns to the project/thread home.
2. If a recoverable startup failure can be produced safely, restore the server and confirm **Try
   again** retries startup without a full reload.
3. Recheck answer-required badges and opt-in thread notifications once each; confirm neither appears
   duplicated.

This receipt does not authorize a push, pull request, main merge, release, or publication. Owner
visual and interaction acceptance remains a separate gate.
