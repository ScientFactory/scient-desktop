# T3 upstream sync through 082e6ea521

Status: locally verified integration candidate. This record does not authorize
a release, enable cloud or mobile publication, or modify `release/stable`.

## Exact boundaries

- Owned base: `e78358aa9080d1a413848e9400fc694f350ae3ff`
- Previous literal T3 boundary:
  `1a4a7596c2c557fcc26a5dceefc04ac6268640b4`
- Integrated T3 target:
  `082e6ea521861fff37b90fcd789b5eaa5ef5d6a6`
- Official tag at the target: none (nearest description:
  `v0.0.34-nightly.20260825.1182-13-g082e6ea521`)
- Exact donor range: `1a4a7596c2..082e6ea521`, 8 commits
- History-preserving merge:
  `cfaffd3223157d5c4cf4e4054cca7feee0c5a219`
- Explicit Claude-compaction deferral:
  `d6a29bc8ad017dd2218717e023f73606e4aaa2de`
- Integration branch:
  `codex/t3-sync-082e6ea5-defer-claude-compaction-20260825`
- Donor write boundary: the `upstream` push URL remains `DISABLED`

The merge commit has the exact owned base and official target as its parents.
No donor commit was replayed, squashed, or cherry-picked. `integrationBase`
advances to the merged target; `reviewedThrough` remains the separate
product-review cursor.

## Adopted behavior

- The macOS launch agent now preserves a safe provider-CLI search path, while
  retaining Scient's service identity and stable launcher.
- Client runtime queries wait through reconnects and retry against the new
  session instead of becoming permanently stale after an interruption.
- Optional providers that were previously used remain enabled across upgrades.
  Scient extends the upstream Cursor, Grok, and OpenCode policy to Droid because
  Droid has the same opt-in default; explicit disables still win. Antigravity is
  not included because it defaults enabled and does not have this migration
  problem.
- A same-repository `preview:mac` label can build an unsigned Apple Silicon DMG.
  Preview versions do not receive an updater publish configuration, and the
  workflow does not obtain signing or release authority.
- Dictation no longer leaves stale thread-jump shortcut hints visible.
- Editing a grouped project name updates the grouped members reliably.
- Chat file chips can reveal files in Finder, File Explorer, or the available
  Linux file manager while retaining Scient's integrated Browser, file panel,
  workspace-image, scientific-rendering, and remote-environment behavior.

## Explicitly deferred behavior

Official commit `c7222ca4dfdea50df1d02ca33710aac5daddbeb7` is present in
literal ancestry, then reversed by the dedicated deferral commit above. The
Claude auto-compaction setting, `/compact` injection, resume-compaction banner,
and associated tests are therefore not active in this candidate. This is a
release-scope decision, not a rewritten merge resolution. After the next
release, the feature can be reconsidered and restored by reviewing and
reverting the dedicated deferral commit against the then-current tree.

Existing generic Claude context-window and provider-reported compaction events
remain available; only the new proactive compaction feature is deferred.

## Conflict composition

The range changed 61 paths and produced nine textual conflicts. Each was
resolved against both parents rather than by choosing a complete side.

- The launchd test keeps Scient's `com.scientfactory.scient.service` identity
  while covering the inherited PATH behavior.
- Claude status keeps Scient's discovered-plus-provider-reported skill merge.
  The upstream compaction command was first composed in the merge and then
  removed only by the explicit deferral commit.
- Server settings retain Scient's secret-store and environment layers while
  gaining the SQLite dependency required for provider-history restoration.
- Server tests and RPC wiring retain Scient's provider lifecycle, fork,
  document, analysis, skills, and voice seams while adding file-manager reveal
  discovery.
- The rich Markdown renderer retains Scient math, BiDi, workspace images,
  scientific fences, Browser/file-preview routing, and copy behavior while
  adopting the upstream primary-action and reveal behavior.
- Chat view and composer conflicts retain Scient's queue, effective skills,
  provider reconnection, and assisted-lifecycle behavior. Compaction-only UI
  state is absent after the explicit deferral.
- Contract tests retain Scient's comfortable-reading defaults while dropping
  only the deferred Claude setting assertions.
- Desktop artifact generation retains Scient identity, native dependencies,
  channel-aware updater publication, and signing behavior; preview versions
  alone suppress updater publication.

The complete test pass found one stale Scient static seam assertion: it still
looked for the pre-refactor HTML-opening expression. The assertion now verifies
the same Browser capability through the new upstream helper and current file
metadata, without weakening the behavior contract.

## Protected-boundary audit

- Scient identity, `scient-next` storage compatibility, protocols, preview
  partitions, client persistence, and legacy-data policy remain intact.
- Managed and system-installed provider paths remain available. Assisted
  install, connection, repair, removal, update, and provider-specific policy
  remain under Scient's lifecycle infrastructure.
- Analysis, LaTeX, local voice, onboarding, browser-PDF export, scientific
  sources, durable forks, skills, and provider lifecycle mounts remain present.
- Telemetry, OTLP, cloud, relay, service activation, updater authority,
  signing credentials, release promotion, and publication gates remain
  unchanged.

## Verification

Local non-visual verification used Node `v24.19.0` and pnpm `11.10.0`:

- The focused merge-seam batch passed 345 tests with 1 skipped test; the
  repaired chat-file seam batch passed 39 tests.
- `pnpm run test` passed all 25 workspace test lanes. The complete server lane
  passed 350 files and 3,727 tests, with 6 files and 29 tests skipped.
- `pnpm run typecheck`, `pnpm run fmt:check`, and `pnpm run lint` passed.
  Lint and Effect diagnostics reported advisory warnings but no errors.
- `pnpm run build`, `pnpm run test:desktop-smoke`, and
  `pnpm run release:smoke` passed.
- `pnpm run lint:mobile` completed its repository checks; optional local
  SwiftLint, ktlint, and detekt executables were not installed, so those three
  external checks remain hosted-CI/platform gates.
- Scient brand, onboarding, LaTeX, analysis, skills, upstream provenance,
  exact ancestry, and `git diff --check` gates passed.

No browser automation, screenshots, visual acceptance, live provider action,
release publication, signing, notarization, or platform packaging was
performed.

## Publication boundary

This merge does not publish artifacts, modify `release/stable`, or enable T3
relay, cloud, hosted web, mobile store, signing, updater, npm, tag, nightly, or
release authority. Those remain explicit Scient gates.
