# T3 upstream sync through `d7819c1881`

Date: 2026-09-23

## Boundaries

- Owned repository: `ScientFactory/scient-desktop`
- Owned base: `38681f131d0c2ab852b56c6d9c4b24b13cc901b6` (latest fetched `origin/main`)
- Previous official integration tip: `aff9318bf46beaf05cc7155b428d3f0b8711efd2`
- Official target: `d7819c18813fa03b033cc1c9472c9acc0ffc0618`
- Official target description: `v0.0.43-nightly.20260922.2096-31-gd7819c1881`
- Exact donor range: `aff9318bf46beaf05cc7155b428d3f0b8711efd2..d7819c18813fa03b033cc1c9472c9acc0ffc0618` (30 commits)
- History-preserving merge: `719f452a4633af276ed9f5e219ced8de43863d49`
- First parent: `38681f131d0c2ab852b56c6d9c4b24b13cc901b6`
- Second parent: `d7819c18813fa03b033cc1c9472c9acc0ffc0618`
- Alignment branch: `codex/t3-sync-d7819c18-20260923`
- `upstream` remains fetch-only; its push URL is `DISABLED`.

Both official `main` refs were checked again before the merge was committed:
owned `origin/main` remained at `38681f131d0c2ab852b56c6d9c4b24b13cc901b6`,
and upstream `main` remained at `d7819c18813fa03b033cc1c9472c9acc0ffc0618`.

## Integrated upstream behavior

All 30 official commits in the donor range are represented in literal Git
ancestry. They fall into four related groups:

- **Provider/model refresh:** the model manifest adds Opus 5.5 without
  rewriting existing aliases and refreshes model metadata. Explicit Cursor
  refresh now bypasses the owned discovery caches; the implementation composes
  that invalidation with Scient's provider skill catalog, managed runtime
  environment, and provider configuration. The corresponding provider and
  manifest regression tests are included.
- **Composer motion:** the composer now observes the shared panel-motion
  settings for draft transitions. The existing Scient draft/timeline behavior
  remains; the merged component reads the hook once and shares its active and
  duration values instead of duplicating setup.
- **Shared UI contracts:** upstream standardized button tones/sizes, menu and
  popover widths, empty states, skeleton shapes, tooltip wrapping/code style,
  textarea growth, dialog rhythm, input typography, sidebar rows/groups, and
  related controls. Those component APIs are applied across chat, settings,
  files, diffs, pull requests, account pages, and other web surfaces. Scient's
  explicit RTL/direction behavior, collision boundaries, provider lifecycle
  messaging, product labels, and specialized keyboard controls remain intact.
- **Lint and device maintenance:** the shared `no-restyle` cleanup reduces the
  measured Scient finding count, and upstream's `agent-device` dependency bump
  to `0.21.12` is included.

## Conflict composition and local qualification fix

Git reported 17 textual conflicts; all were resolved and reviewed. The paths
were `CursorDriver.ts`, `ChatMarkdown.tsx`, `ChatView.tsx`,
`CommandPalette.tsx`, `AssistantCitationChip.tsx`, `ChatComposer.tsx`,
`MessagesTimeline.tsx`, `ModelPickerSidebar.tsx`, `ProviderStatusBanner.tsx`,
`MobileClientsUserProfilePage.tsx`, `WelcomeWizard.tsx`,
`KeybindingsSettings.tsx`, `ui/input.tsx`, `ui/popover.tsx`,
`ui/scroll-area.tsx`, `_chat.index.tsx`, and
`scripts/lint-restyle-ceiling.ts`. The range touched 155 paths, 70 of which
overlapped Scient changes; the other auto-merged overlaps were reviewed too.

Important compositions:

- `CursorDriver.ts` retains Scient's managed environment and skills/catalog
  behavior while adopting the explicit model-discovery `discover` and
  `invalidate` path. The provider refresh tests exercise this boundary.
- `ChatMarkdown`, `Popover`, and `ScrollArea` retain Scient's tooltip/file
  presentation, overlay collision boundary, and explicit RTL direction while
  using upstream's new shared width/radius contracts.
- `ChatView`, `ChatComposer`, and the draft-transition helper preserve
  Scient's current conversation/draft behavior while using upstream's motion
  settings. The composer has one shared motion-settings read.
- `MessagesTimeline` keeps Scient's current queue/timeline model. An older T3
  queued-row shape is not part of this donor range and is incompatible with
  the current Scient row contract; it was not reintroduced. The actual new
  `WorktreeSetup` popover width change is retained.
- `WelcomeWizard` keeps Scient's display identity, server package selection,
  and `SCIENT_SERVER_PACKAGE`-based connect command; it does not restore the
  generic T3 `npx t3 connect` command.
- `ProviderStatusBanner`, `ModelPickerSidebar`, `KeybindingsSettings`, and
  account/chat surfaces preserve Scient lifecycle filtering, provider setup,
  platform-aware shortcuts, and visible Scient identity alongside the new
  shared primitives.

The recursive test run exposed a real test dependency gap: the relay test
imports `alchemy/Test/Vitest`, whose implementation requires Vitest, but the
relay did not declare that test peer. The alignment adds Vitest as a relay
dev dependency and an optional peer extension for the pinned Alchemy version.
This is test-only; it changes no runtime/product dependency behavior. The
relay's full 286-test suite then passed. The no-restyle ceiling is now the
measured `1127` rather than the stale `1727`; it remains an active, measured
guard rather than being disabled or widened.

## Protected boundaries

- Product identity, provider authority/lifecycle, Scient-specific project and
  environment behavior, and release/publication authority remain owned by
  Scient.
- No release, cloud deployment, or publication workflow was enabled.
- No migration identifier was changed or renumbered.
- The upstream remote remains fetch-only.
- No visual acceptance or production release is claimed in this receipt; a
  separate candidate app is provided for the user's manual UI review.

## Verification

Passed on the exact merge tree (`a9474f71bab44321de739bf70239ec21d3f555ae`):

- `pnpm lint` — exit 0; existing advisory warnings remain.
- `pnpm run typecheck` — all 29 workspace projects, exit 0.
- `pnpm exec vp run -r --concurrency-limit 2 test` — all workspace suites,
  exit 0. Web: 749 files / 8,756 tests passed. Server: 528 files / 7,644
  passed, 22 files / 73 tests skipped. Desktop: 117 files / 1,448 passed,
  4 files / 43 tests skipped. Mobile: 185 files / 1,696 passed. Relay: 30
  files / 286 passed.
- `pnpm build` — exit 0, including web, server, and desktop packaging builds.
- `pnpm test:desktop-smoke` — passed.
- `pnpm brand:check` — passed across 2,210 product-surface files.
- `pnpm lint:restyle-ceiling` — 1,127 findings, at the measured ceiling.
- `pnpm lint:mobile` — static check passed across 14 Swift and 26 Kotlin
  source files. Optional SwiftLint, ktlint, and detekt executables were not
  installed and therefore were skipped.
- `pnpm fmt:check` — all 5,675 files formatted.
- Alignment seam audit — onboarding, skills, analysis, and LaTeX checks
  passed; zero reference-snapshot paths were present.
- `git diff --check`, `git diff --cached --check`, and the merge-index check
  passed; zero unmerged entries remained.

The first unconstrained recursive test invocation exceeded this machine's
memory budget and also exposed the relay's missing Vitest test peer. The final
bounded run above is the authoritative complete result. Desktop's Electron
binary was restored with the package-scoped rebuild before the successful
desktop and smoke tests. The production build emitted non-fatal upstream/tool
warnings for large web chunks, MathJax's direct `eval`, the optional Linux
`x11` import, and CommonJS `import.meta` handling.

## Publication boundary

The history-preserving alignment is committed and verified on its isolated
branch. It is delivered as an unmerged draft pull request for review; no
auto-merge, merge, or release action was taken.
