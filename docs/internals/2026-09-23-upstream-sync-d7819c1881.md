# T3 upstream alignment through `f5ef0ddb90` (PR #350)

Date: 2026-09-23

This receipt records the continuing alignment on the existing PR #350 branch.
It supersedes the earlier `d7819c1881` checkpoint; Git history retains that
checkpoint and its review record. No new alignment branch was created. The
history-preserving merge and clean CI workspace gate now support advancing
`upstream-state.json` and the `UPSTREAM.md` integration pointer to f5ef.

## Frozen boundaries and history

- Owned repository: `ScientFactory/scient-desktop`
- Original owned base: `38681f131d0c2ab852b56c6d9c4b24b13cc901b6`
- Previous official integration tip: `aff9318bf46beaf05cc7155b428d3f0b8711efd2`
- Previous checkpoint: `d7819c18813fa03b033cc1c9472c9acc0ffc0618`
- Final official target: `f5ef0ddb90a8c36584e181b1913e7b8a5df30ffc`
- Final official target description:
  `v0.0.43-nightly.20260923.2135-14-gf5ef0ddb90`
- Complete donor range `aff9318bf46beaf05cc7155b428d3f0b8711efd2..f5ef0ddb90a8c36584e181b1913e7b8a5df30ffc`:
  **72 official commits** (including **42 added since the d781 checkpoint**)
- Initial history-preserving merge: `719f452a4633af276ed9f5e219ced8de43863d49`
  (first parent original owned base; second parent exact d781 target)
- Extension merge at `10882bea739a98f11b942c20c6570eb6126dd010`:
  `f741668a6d31ec6681d6324a0f6a7b97f205f06e`
- Extension merge at final target `f5ef0ddb90a8c36584e181b1913e7b8a5df30ffc`:
  `34f081f99f368f9eaca0af29266ec13530956f77`
- Owned-main catch-up merge: `ea6af93176134c52d76eadc7a33cc5fabeb714d7`,
  whose second parent is fetched `origin/main` at
  `a86cec5fd747fe7937241649bc6a500e2d64516a`.
- Alignment branch: `codex/t3-sync-d7819c18-20260923`
- `upstream` remains fetch-only; its push URL is `DISABLED`.

Both remotes were fetched and rechecked for this extension. At review, owned
`origin/main` was `a86cec5fd747fe7937241649bc6a500e2d64516a` and official
`upstream/main` was `f5ef0ddb90a8c36584e181b1913e7b8a5df30ffc`. The candidate
contains the exact current owned main and all three official alignment merge
parents above. The complete-range historical plan measured 72 donor commits,
381 changed paths, 135 overlaps, zero reference snapshots, and 43 _predicted_
textual-conflict paths. Those are plan estimates, not a claim that 43 actual
conflicts occurred. The worktree has no unresolved index entries.

## Integrated behavior

The first 30 commits through d781 remain part of the same donor history. They
add provider/model refresh and Opus metadata, shared composer-motion settings,
large shared UI contract updates, and device maintenance. Their composition
preserves Scient's managed provider/runtime, draft/timeline, RTL and overlay
boundaries, product identity, project/environment policy, and updater/release
authority. The 42-commit extension adds, among other changes:

- **Provider and model lifecycle:** provider compatibility-range advisories
  and shared authentication/credential-flow improvements; model discovery and
  refresh behavior remains composed with Scient's cache invalidation, managed
  runtime catalog refresh, provider configuration, and compatibility metadata.
- **GitHub pull-request reliability:** improved PR generation/batching and
  quota behavior, plus fixes to probing candidate branches before operations.
- **Mobile and cross-platform behavior:** home/thread-list and settings
  refactors, ordering/performance and error-boundary improvements, safe
  platform-specific behavior retained after an upstream regression revert,
  and desktop WSL/Linuxbrew Node discovery fixes.
- **Navigation and shared UI:** settings scope/breadcrumb refinements, nested
  chat scrolling, keyboard back/forward navigation, title-match ordering,
  and broad component-contract cleanup. Upstream removes the old no-restyle
  lint ceiling; the alignment does not recreate that gate or impose it on
  upstream's component API changes.
- **Model defaults and maintenance:** the upstream model-generation default
  change and the remaining dependency/device-maintenance updates are included.

All official commits remain represented in Git ancestry; this grouping is a
behavior summary, not a selection of commits to import.

## Composition and targeted local corrections

The original d781 merge's 17 textual conflict resolutions remain as recorded
in the previous version of this receipt in Git history. Subsequent upstream
extensions and the owned-main catch-up were reviewed in the same branch. The
catch-up's important semantic compositions include:

The original conflict set was `CursorDriver.ts`, `ChatMarkdown.tsx`,
`ChatView.tsx`, `CommandPalette.tsx`, `AssistantCitationChip.tsx`,
`ChatComposer.tsx`, `MessagesTimeline.tsx`, `ModelPickerSidebar.tsx`,
`ProviderStatusBanner.tsx`, `MobileClientsUserProfilePage.tsx`,
`WelcomeWizard.tsx`, `KeybindingsSettings.tsx`, `ui/input.tsx`,
`ui/popover.tsx`, `ui/scroll-area.tsx`, `_chat.index.tsx`, and
`scripts/lint-restyle-ceiling.ts`. They were resolved by composing the current
Scient behavior with upstream's mechanics; upstream's later removal of the
no-restyle ceiling is retained rather than restoring the gate. The original
recursive test run also exposed a missing relay test-only Vitest peer; the
alignment added the test dependency (no runtime dependency change), after
which the relay suite passed 286 tests.

- `apps/server/src/ws.ts`: keep the owned provider-cache invalidation and the
  managed-runtime-catalog refresh as separate refresh effects.
- `apps/server/src/provider/model-manifest.json`: take upstream's current
  update timestamp while retaining Scient compatibility metadata and newer
  discovered-model statuses.
- `apps/web/src/components/ProviderUpdateLaunchNotification.logic.test.ts`:
  preserve both the current module path and the relevant expectations.
- `docs/internals/model-manifest.md`: retain the updated upstream guidance
  while preserving Scient's compatibility and legacy-provider notes.

Review also confirmed that managed Codex remains manual-only for provider
maintenance and is not surfaced as an installable update candidate; no broad
guard was added that would block unrelated managed-provider capabilities.

Two narrow implementation/test-quality repairs were needed to qualify the
combined tree:

- In `apps/web/src/scient/sources/SourceEditor.tsx`, five manually controlled
  validation messages now use Base UI `FieldError` with `match={true}`. Its
  default native/form validity matching does not display externally controlled
  errors; the explicit match keeps the existing validation feedback visible.
- Provider compatibility-advisory copy now says “Scient release” on the
  product surface, while retaining the internal upstream-compatibility field
  name. `brand:check` then passed.

Eight web tests were updated to assert current semantic/native behavior rather
than removed implementation details such as stale class strings, obsolete
`data-slot` placement, or unsupported matcher APIs. No production styling was
changed to satisfy those assertions.

The first CI run exposed two further integration defects. The unused-code gate
found obsolete composer chip constants and a redundant citation-component
alias left after the upstream shared-chip migration. Removing the dead exports
and using the upstream `AssistantCitationChip` name at the remaining Scient
call sites preserves behavior while restoring the gate. The browser layout
suite reloaded before assertions when Vite discovered `@base-ui/react/toggle`
late; including that dependency in its existing test optimizer list makes a
fresh optimization stable. No Markdown performance threshold was changed.

## Protected boundaries

- Scient product identity, provider authority/lifecycle, scientific behavior,
  and release/publication authority remain Scient-owned.
- No release, cloud deployment, or publication workflow was enabled.
- No migration identifier was changed or renumbered.
- The official upstream remote remains fetch-only.
- Visual acceptance and production release are not claimed. PR #350 remains
  an unmerged draft for review.

## Verification

Passed on the current composed candidate, except where explicitly qualified:

- Full web suite: **751 files / 8,763 tests passed**.
- The eight initially affected web test files: **138 tests passed**; focused
  provider compatibility tests: **23 passed**; provider update notification
  logic tests: **46 passed**.
- All 29 workspace projects passed `pnpm run typecheck`.
- `pnpm exec vp lint --report-unused-disable-directives` passed (existing
  advisory warnings remain); `pnpm exec vp fmt --check` passed after format.
- `pnpm run build` and `pnpm run test:desktop-smoke` passed. Build emitted
  non-fatal existing/toolchain warnings for optional `x11`, CommonJS
  `import.meta`, large chunks, and MathJax.
- `pnpm run brand:check` passed across 2,216 product-surface files.
- `pnpm run lint:mobile` passed its static checks across 14 Swift and 26
  Kotlin files. Optional SwiftLint, ktlint, and detekt were unavailable and
  skipped.
- `pnpm alignment:seams:check --base 38681f131d0c2ab852b56c6d9c4b24b13cc901b6
--upstream-ref f5ef0ddb90a8c36584e181b1913e7b8a5df30ffc` passed the
  onboarding, skills, analysis, and LaTeX checks; there were no reference
  snapshots.
- Focused Markdown performance qualification passed **5/5** when isolated.
- After the CI repairs, `pnpm run knip:check`, all 29 typecheck projects, lint,
  formatting, focused citation/composer-chip tests (**9/9**), and the web layout
  suite (**6/6** after fresh dependency optimization) passed locally.
- On repaired commit `8bce755e9c`, [CI run 35839193561](https://github.com/ScientFactory/scient-desktop/actions/runs/35839193561)
  passed, including `Check`, `Test Web layout`, and `Test Workspaces`; the
  workspace result includes the Markdown performance gate. The first CI run
  had already passed the other web, server, provenance, release-smoke, and
  native test jobs; the repaired run requalified them.

The full recursive workspace test run had one load-sensitive failure while
other workspace suites passed: the 100 KiB Markdown typing test measured
68.189 ms against its fixed 64 ms limit under concurrent load. The test was
not weakened. Its isolated rerun passed, reporting 5.04 ms standard-source
p95 and 10.48 ms large-source p95; clean CI `Test Workspaces` passed. Optional
mobile linters and visual/manual acceptance remain separate gates.

## Publication boundary

This is a continuation of the existing history-preserving alignment on PR
#350, not a new alignment branch. Keep the PR open and unmerged; do not enable
auto-merge or start release/publication work as part of this alignment. The
candidate contains f5ef ancestry and its integration pointer records that
exact target after the clean workspace gate passed.
