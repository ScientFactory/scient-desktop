# T3 alignment through f814983c

Status: automated qualification passed; owner visual review pending.
This is one complete, bounded alignment, not a selection of upstream changes.

## Frozen history

- Owned base: `7cefb603ef43fe0c4555826ceeb67b0e2338c668`.
- Previous official integration: `6c583620ff7ad3235b135af7107c0543467eecfa`.
- Official target: `f814983c262b42bd79247bae377a709925c70d63`.
- Official range: 23 commits, including the Android inline-icon change and its later revert.
- Target describe: `v0.0.41-nightly.20260910.1486-6-gf814983c26`.
- Branch: `codex/t3-sync-f814983c-20260910`.
- History-preserving merge: `b4fd31d503250e89bdd103f111ff20a16395e70a`, with the owned base and exact
  official target above as its first and second parents.
- `upstream` remains fetch-only (`https://github.com/pingdotgg/t3code.git`, push `DISABLED`).
- Canonical owned main was rechecked against fetched `origin/main`; both remain at the owned base.

## Adopted upstream behavior

| Area                       | Included behavior                                                                                                               | Upstream PRs                                   |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| Thread pull requests       | Multiple explicit links; link/list/unlink MCP tools; linked-thread search; project-identity repair for legacy relinks           | #10839, #10870, #11045                         |
| GitHub stacks              | Stack navigation, merge/rebase actions, stack snapshots and thread associations                                                 | #10875                                         |
| PR reads and presentation  | Recent reads survive restarts within their original TTL; diff counts; simplified multiple-link badges; stronger primary actions | #11007, #10609, #11104, #11105                 |
| Models                     | Enable/disable all built-in models without discarding custom or unknown hidden selections; composer names use available width   | #10947, #11002                                 |
| Chat and media             | Independent expansion of duplicate tool commands; expanded-image zoom/pan; touch-accessible copy buttons                        | #10981, #10869, #11020                         |
| Panels and diffs           | Closing a preview returns it to picture-in-picture; optional blue/orange diff palette                                           | #11102, #10671                                 |
| Desktop integrations       | Linux terminal middle-click paste; remote Zed project opening                                                                   | #11018, #11022                                 |
| Mobile and thread activity | Android glass fallbacks and row-overlap fixes; quieter settled threads; final upstream inline-icon behavior after revert        | #10964, #10983, #10998, #11079, #11098, #11101 |

All official commits in the frozen range remain literal ancestry. This does not incorporate open
upstream PRs or keep moving the target while qualification is running.

## Conflict composition and review findings

The initial merge reported 39 textual conflicts. Both sides and auto-merged overlaps were reviewed.

- **MCP and provider awareness:** retain Scient's explicit capability sets, immutable per-session
  copies, skill scope, Sources, Documents, and optional browser access. Add the upstream PR toolkit
  to the existing baseline capabilities. Do not replace the registry with upstream's narrower
  preview boolean or create a second browser-policy source. Shared runtime instructions adopt the
  new PR-linking guidance with Scient product identity; `t3-code` remains the MCP compatibility name.
- **Persistence and snapshots:** preserve fork lineage, latest completed answer, and historical
  workspace/project decoding while adding PR arrays and old-client derived single-link data. Audit
  query/destructuring order across full snapshots, active/archived shells, and individual details.
- **Retired projectless records:** new PR links and MCP handlers reject records without an owning
  project; background sync ignores them and legacy reference derivation is null-safe. Historical
  records remain decodable. This does not restore projectless conversation creation or UI.
- **Rich chat:** retain Scient Markdown/code/rich-fence rendering, content direction, media actions,
  copy/download, SnapShot accessibility content, and composer focus ownership. Add upstream image
  zoom inside the existing dialog. Keyboard tests cover the pan-versus-gallery contract.
- **Panel navigation:** preserve Sources, Compute, file/PDF/editor surfaces and pending-save guards.
  The new PR-list entry and preview-close/PiP transition use the existing guarded navigation path.
- **Settings and provider lifecycle:** retain assisted connection and managed runtime paths,
  provider-instance identity, separate subscription/email presentation, onboarding entry points,
  existing defaults, and Scient settings ordering. Compose the new bulk model controls and diff
  palette without rewriting these policies.
- **Compatibility and tests:** retain Scient fork/bootstrap fixtures and old-event decoding tests;
  extend fixtures with the new PR-array field. Correct duplicate imports and a dropped setup line
  produced by text composition. Do not delete tests to make the merged types pass.

## Targeted migration correction

Scient already records migrations 51 (branch PR metadata) and 52 (active order). A retired
development migration used 50 for a session-error-reason column. The migration runner advances past
the highest recorded ID: registering upstream's new multi-PR step as 50 could therefore silently
skip it on existing Scient databases.

The incoming SQL is registered as **53, `ProjectionThreadPullRequests`**. Existing migration files,
IDs, recorded names/timestamps, the runner, and the separate `scient_schema_migrations` ledger are
unchanged. The gap at 50 stays reserved; there is no destructive repair or historical renumbering.

Synthetic SQLite tests cover fresh initialization, upgrades from 49, the retired development-50
state, and 52; exact preservation of existing ledger rows and session data; legacy PR-link backfill;
malformed/unknown-host and separate Azure-organization links; and no-op repeated startup. No live
or copied user database was used.

## Protected boundaries

No changes to workflow permissions, signing, release/publication controls, telemetry consent,
cloud/relay activation, mobile distribution, production identity, or production state roots.
Dependencies remain locked; package manifest changes expose upstream's new shared modules only.
Scient provider setup, fork, Skills, Sources, voice, compute, PDF, LaTeX, and rich Markdown remain
on their existing owners. The checks below are structural/behavioral evidence, not visual acceptance
or live-provider/platform qualification.

## Qualification

- Locked dependency installation, formatting, lint, full typecheck, full build, dependency/export
  checks (`pnpm knip:check`), brand check, desktop smoke, and release smoke: passed.
- Analysis, onboarding, Skills, and LaTeX seam checks: passed for the committed range. Provenance
  verification passed using the exact owned base and frozen official target above.
- Full test suite: `VITEST_MAX_WORKERS=2 pnpm run test` passed across all 27 tasks, including
  7,017 web tests and 6,391 server tests. Existing skipped tests remain explicit, including 63 in
  the server suite. Earlier default-concurrency runs exceeded
  unchanged Markdown timing budgets; one also timed out a theme-module import while another
  worktree ran its full suite. No threshold, timeout, assertion, or production code was relaxed.
- Dedicated `SCIENT_MARKDOWN_PERF_STRICT=1` run: all five performance tests passed.
- Additional nonvisual image-zoom tests: all four passed, including keyboard/ref pan ownership,
  modified/repeated keys, wheel limits, resize reset, accessible labeling, and error delegation.
- Mobile native static command completed, but SwiftLint, ktlint, and detekt were unavailable and
  explicitly skipped. No Android/iOS device acceptance or Linux/Windows runtime test was performed.
- No computer use, live-provider messages, real PR mutations, release dispatch, or publication.

## Owner manual review

Use a new isolated candidate state; do not copy the installed app's database or credentials.

1. Open a project and use normal chat/fork flows. Check provider setup and reasoning selection,
   separate subscription/email lines, and enable/disable-all models in provider settings.
2. Link two PRs to a conversation, inspect their list, unlink one, and find conversations by PR.
   Review stack navigation on a disposable stack. Only test merge/rebase when intentionally ready
   to mutate that test stack.
3. Expand an image; zoom, pan, return to fit, navigate images, copy/download, and close back to the
   composer. Check Markdown/rich fences and repeated tool-command expansion.
4. Close a browser preview and confirm PiP. Switch between PRs and file/Markdown/PDF/Sources/Compute
   surfaces; unsaved editor changes must still receive the existing save/discard protection.
5. Try the blue/orange diff palette, settled-thread presentation, and the normal PR actions.

This receipt does not authorize a main merge, release, or publication. No push or PR is implied;
the owner will review the isolated candidate first.
